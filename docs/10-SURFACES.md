# 10 · Surfaces — 什么真的跑通了

> 这张表是项目状态的**唯一可信来源**。文档写了不等于跑通了。
> 状态只有五档：`stable`（跑通且有证据）/ `experimental`（能跑但没稳）/
> `stub`（只有占位/签名）/ `spec-only`（只有文档）/ `archived`。
>
> 规则：**动完代码就更新这张表。** 证据一栏必须是"跑过的命令"或"截图路径"，不能是"应该可以"。

最后更新：2026-09-19

<details>
<summary>改动史（每次动完代码追加一行）</summary>

这一段原来是十几条堆在一起的「最后更新」，其中三处**两条被拼在同一行上** ——
它不再是一个时间戳，是一堆。一个谁都看不出"现在是哪一版"的时间戳，
和没有时间戳是一回事（docs/02 P21）。

**日期按写下这一行的那次提交算**，不按感觉：`git blame -L <行> docs/10-SURFACES.md`
（或者拿这一行的原话去 `git log -S`）给出的作者日期就是它。一行日期如果对不上
它描述的那次提交，这张表就不再是记录，是一段回忆 —— 2026-09-13 那天晚上
有七行写成了 2026-09-14（当时还没到那一天），它们是按上面这条重新落回去的。
对不上任何提交的行**宁可留着不动**：凭感觉补一个日期等于伪造一条证据。

| 日期 | 这一版落地了什么 |
|---|---|
| 2026-09-19 | 公开站发现层从「只有 title / favicon / viewport」变成单一契约驱动：8 张展陈页、`/404` 和 18 张 `/dev/*` 工作台在初始 HTML 里有唯一 description / canonical / robots / Open Graph / Twitter Card / `Organization + WebSite + VisualArtwork + WebPage` JSON-LD，全部依产品裁定允许索引；`/poster/*` 与未登记 HTML 仍缺省 `noindex`。同一页面表生成 robots / sitemap / manifest / llms / llms-full / IndexNow key，社交图用真实舞台证据裁成 1200×630，不用概念插画代替作品。`discovery.test.ts` 8/8，`npm run build` 通过，XML / manifest 另独解析通过；构建只有既有 Vite native-config、字体运行时解析与 kiosk >500kB 警告。**`/404` 的 index 是开放许可不是收录承诺，搜索引擎仍可自行判它为 soft-404；这些证据只证明产物可被发现，不证明 Google / Bing / 大模型已经收录；Search Console 所有权、组织官网反链和部署后 IndexNow 仍是外部操作** |
| 2026-09-18 | Auto Framing 新增 app 层唯一的“有效景别”解析：分类器 / 手动策略仍保留原始决定，实际身体方案与真正画上台的 companion 再统一折成舞台景别；舞台、横向快档、纵向跟随、小屏数字裁切与 HUD 不再各自猜一次。`rig` / `stub` / `towering` 的比例变化仍有可读上半身，不能因 `drift > 0` 被误判成非人形；四足 / 环 / 柱 / 团块与未知形体一旦开始漂移则回全景。多人以渲染结果而非探测候选为准，调速器只留主身体时小屏不会误报第二具身体；工作台多人模式与正式链同样让横向控制归中。类型检查、42 条定向测试、9 段 60Hz 连续性 trace 与 `npm run build` 已过；构建只有既有 Vite native-config、字体运行时解析与 kiosk >500kB 警告。全仓首跑暴露的呈现骨架旧源码断言已改为守 `presented.skeleton` 语义，慢回路真实 HTTP 夹具显式绑定 IPv4 并清理 keep-alive；最终 `npm run check` 为 core 362 / app 724 / parts 247 件、0 错 12 个既有警告。**真人近 / 中 / 远、多人进出、调速器降级观感及原生 `faceFraming` 组合仍未验证** |
| 2026-09-18 | 舞台新增“呈现骨架”回执边界：`World.skeleton` 继续只保存观众输入，Director 的 echo / resist / facing 输出只有在身体 `pose()` 成功返回后才成为 last-good；接触阴影、落脚声、取景、多人交接旧身体和 HUD 现在消费同一副真正交给身体的骨架，不再把变化前的人体输入与画面混用。reset 清回执后立即用参考姿态重新立基准，pose 异常保留 last-good；包装器每帧只保存对象引用，不复制骨架，并保留物种到场后会切换的动态 stats getter。定向测试 30/30、`npm run typecheck` 与 `npm run build` 通过，构建只有既有 Vite native-config、字体运行时解析与 kiosk >500kB 警告。一次全仓 `npm run check` 中 core 362/362，通过；app 715 中 713 通过、2 失败，输出明确捕获到慢回路 HTTP 在全负载下的一次 `fetch failed`，同文件隔离复跑 8/8；因此这次全仓门禁**不记为通过**，需要在本轮合并前定位并重新跑绿。真人摄像头、目标 WebGPU 和最终像素仍未验证 |
| 2026-09-18 | 前端到访存档补齐 encounter epoch 边界：上一位迟到的成功、reject、500、坏 JSON 或 404→Worker fallback 仍可完成那次已经走完弧线的 best-effort 写入，但它们不再能把下一位的 `phase / n` 改成 `kept / off`；当前相遇自己的失败仍按 §7.1 静默关闭整次会话。`ok: true` 现在还必须带正的安全整数序号，否则不能伪装成已保存；平台 idle 调度器同步抛错也只关存档，不再逃进帧循环。24 条前端存档测试逐条覆盖旧 / 新 epoch、延迟 idle、两处端点与坏回执；`npm run check`：core 362 / app 707 / parts 247 件、0 错 12 个既有警告；`npm run build` 通过，只有既有 Vite native-config、字体运行时解析与 kiosk >500kB 警告。**线上 Worker / 真 D1 仍未部署或实连；这刀不增加任何运动数据字段，也不改变不存 IP 的裁定** |
| 2026-09-18 | 脱离从“一场只放一只手”扩成全槽位但有秩序的变化语法：每场第一件仍必须是手，随后按乐章、概率与接纳间隔逐步开放头 / 手 / 脚、四肢 / 颈肩、脊柱身体与单个具名关节盖片；terminal 占 1 份视觉预算，segment / core 占 2 份，结构性脱离期间不再叠加第二场替换，满预算也不会驱逐已经在途的件。所有轨迹都从当帧 socket 出发并回到当帧 socket，位移按骨长或米制 joint offset 计算；伴随身体不复制主身体脱离。renderer 只有真正接纳后才回 receipt，策略才 commit；排队、冲突或容量拒绝不偷耗机会。交接结束保留当前缩放 / 高度而不半途跳回全尺寸；触地点在 receipt 后取样，只剔除对应脱离骨的共享端点，取景仍读完整骨架。真实 `parts.json` 扫 17 个骨槽位 + 13 个 joint cap × 全进度，短命几何逐件守地；15/30/60/120Hz、坏数、全槽位可达、单 joint、两脚冲突、结构队列和换人清状态均有回归。`npm run check`：core 362 / app 701 / parts 247 件、0 错 12 个既有警告；`npm run build` 通过，只有既有 Vite native-config、字体运行时解析与 kiosk 体积警告。**真人近 / 中 / 远、脱离可读性、身体脱离的审美强度与目标 WebGPU 仍未跑；旧自由 radial 继续封存，return 只保留类型位、未开放策略** |
| 2026-09-18 | 自动人数不再能被一只永不返回的 MediaPipe `setOptions()` 永久掐住：worker 与主线程降级现在共用一份 applied / desired / in-flight / deadline owner，普通 reject 仍释放互斥并保留旧图，同值可显式重试；等待超过 `CAPTURE.peopleReconfigureTimeoutMs=2500` 则进入终态，不能假装解锁后继续推理。worker owner 会终止整份 worker、清全局缓存并让当前 Capture 退回 replay；主线程 owner 会把旧 landmarker 从快回路摘掉，等旧 mutation 真 settle 后才 close，迟到回执不能推进 applied 或启动下一目标。新纯状态测试覆盖成功、latest-wins、同步发送失败、普通拒绝、同值重试、永不返回、超时后迟到与 stop 在途；接线守卫覆盖两条 owner 的资源隔离。`npm run check`：core 362 / app 694 / parts 247 件、0 错 12 个既有警告；`npm run build` 通过，只有既有 Vite native-config、字体运行时解析与 kiosk >500kB 警告。**真人 / 真 MediaPipe 慢挂未注入；2500ms 是工程安全界，不是设备实测；主线程 Promise 若真的永不 settle，那份已隔离的 WASM 对象只能等页面卸载回收，但不会再挡画面** |
| 2026-09-18 | Auto Framing 工作台不再用一套“看起来像正式链”的假输入：分类器现接真实 viewport aspect，横向控制接 `upper` / `cameraFraming`，纵向不再把 world 关节高度写死为 0；调用方可传正式稳定化后的同名关节高度，只有原始姿态时才用同一骨架映射做有限近似。HUD / trace 直接保存分类器 reading 与策略 decision，不再伪造 `inMode=0`、`evidence=null`、`trend=NaN`。9:16 画幅、上半身快档、低质量 + 系统取景回中线、screen/world 同步抵消与坏 dt / 坏画幅 / 残缺点逐字段回归已过；顺带修掉开机第一帧就跟丢且还没有 accepted scale 时 deadZone / band 变 NaN 的核心边界。九段 node/browser trace 全部通过；`npm run check`：core 362 / app 690 / parts 247 件、0 错 12 个既有警告；`npm run build` 通过，只有既有 Vite native-config、字体运行时解析与 kiosk >500kB 警告。工作台仍是手抄接线、原始 world 重建仍是近似，真人近 / 中 / 远与逆光切换仍未跑 |
| 2026-09-18 | Auto Framing 的纵向控制不再在 screen 基线过期后偷偷切回 world 坐标：现代链里 `screen === null`（明确丢失）或有 screen 但低质量、且已没有可信旧基线时，目标持续为中线 0；只有 `screen === undefined` 的旧回放保留 world-relative fallback。旧逻辑的“正常 → 丢失到过期 → −8cm world fallback”反证会滑到 −79.86mm，修后在 3 秒坏光重现里保持中线 5mm 容差；已有基线的坏光仍冻结，短丢失仍冻结，长丢失仍平滑归中，没有调任何 tuning。`npm run check`：core 361 / app 687 / parts 247 件、0 错 12 个既有警告；`npm run build` 通过，只有既有 Vite native-config、字体运行时解析与 kiosk >500kB 警告。真人近 / 中 / 远、逆光与模式切换观感仍未跑 |
| 2026-09-18 | 变化弧线第一次有了明确而克制的“脱离”语法：每场相遇只允许**第一件真正被接纳替换的手**离开再回接一次；脚、整条胳膊和其他槽位即使误请求也会在渲染边界降级为原位替换。策略用 plan / commit 两步，排队或被预算拒绝的替换不会提前耗掉机会，换观众时和其他 encounter 状态一起清零；只有主身体播放 release，伴随身体继续原位变化。手在原有 1.2s 交接时钟上走 `0 → 0.55 × 当帧手骨长 → 0`（标准 108mm 手约 59mm），每帧仍挂在当前骨骼上，不新增桶、实例或 draw。15/30/60/120Hz 均按时归位，默认原位路径不变，NaN / ±Infinity 进度有限；真实 `wheelleg` seed 92 tier 2→3 的 `quadruped` 右手夹具对原位 / release 各扫 121 阶段，效果最低点不穿地、稳定身体不被抬。`npm run check`：core 360 / app 687 / parts 247 件、0 错 12 个既有警告；`npm run build` 通过，只有既有 Vite native-config、字体运行时解析与 kiosk >500kB 警告。真人链的可读性 / 时机未跑；当前不强行保证 20–50s 出手，也不开放整臂脱离 |
| 2026-09-18 | 变化的安全缺省改成“原位发生”：忒修斯 `replace` 保留描边连续所需的缩放 / 墨屑曲线，但新件不再对所有槽位隐式从 150mm 外飞回；未来只有显式单手 release 策略能授权脱离。主体落地后，每个 `groundsBody=false` 效果实例再独立测自己的 AABB，最低点穿地时只沿 +Y clamp 自己，不反向搬动主体。`wheelleg` seed 92 tier 2→3、`quadruped` 右手夹具扫 121 个阶段：真 GLB 事前测得可见新手最深 −74.335mm，修后 `parts.json` 同件夹具全程最低点 −0.000mm（浮点噪声），稳定头部矩阵与不画效果的对照逐位一致；元数据与 GLB 的 AABB 最大差 0.033mm。NaN / ±Infinity 进度现在也只退到 0，不产生非有限 scale。`npm run check`：core 360 / app 683 / parts 247 件、0 错 12 个既有警告；`npm run build` 通过，只有既有 Vite native-config、字体运行时解析与 kiosk >500kB 警告。仍欠：一次一只手的显式 release、真 GLB 全进度回归与真人可见链 |
| 2026-09-18 | 全身 remorph 的队列现在分清“最终目标 genome”和“此刻已经轮到的画面状态”：未取得交接名额的槽位继续画 `Swap.from`，只有进入 active 才开始旧→新；主体和伴随身体共用同一语义。真实 porcelain seed 11 tier 1→2 一次改 11 个槽位，描边预算只让 2 个 active、9 个排队；新回归直接量 three 的实例桶，证明所有目标件在激活前 count=0、旧件不消失，队列走完后 11 个槽位全到目标。实现用不分配的最多 18 项线性查找，不为微小性能优化再建一张可能与队列漂移的镜像 Map。`npm run check`：core 360 / app 681 / parts 247 件、0 错 12 个既有警告；`npm run build` 通过，只有既有 Vite native-config、字体运行时解析与 kiosk >500kB 警告 |
| 2026-09-18 | 替换槽位的主体落地基准不再把旧件 AABB 用到事件最后一帧：`assemble()` 同时收旧 / 新满尺寸、在插座上的非渲染代理，每个槽位按新件已有的 `graftCurve` 插值世界最低点，多槽位不共用一个假进度。真实 `wheelleg → screenface` 左脚回归的旧路径终点跳 45.29mm，1.2s / 60Hz 连续代理的最坏单帧收到 0.943mm，终点逐位等于正常新身体；NaN 进度只退回旧端，不污染帧循环。这一刀不增加可见实例 / draw，但交接中多一次最多两个槽位的纯挂载 / AABB 测量。`npm run check`：core 360 / app 680 / parts 247 件、0 错 12 个既有警告；`npm run build` 通过，只有既有 Vite native-config、字体运行时解析与 kiosk >500kB 警告。仍欠：短命效果几何自己的 clamp / fade；真 GLB 回归已量到四足右手作为承重前足时一度穿地 74.335mm，不把这一刀冒充整条变形已完成 |
| 2026-09-18 | 有跨帧记忆的玩法现在由每个 `Director` 私有实例化：`ACTS` 只登记原型，无状态玩法继续零额外实例；`untether` 的交还姿态与相位从模块全局移进工厂闭包，两个 Director 在同一进程交错运行时各守自己的 x=0 / x=2 基准，坏 dt 也只走有限 fallback。摇曳频率、幅度、高度权重与卡顿步长全部集中到 `tuning.ts/UNTETHER`，后续换声音、段落速度或多舞台不用改控制流。静止 20 秒对照仍自主移动 102.9mm；`npm run check`：core 360 / app 679 / parts 247 件、0 错 12 个既有警告；`npm run build` 通过，只有既有 Vite native-config、字体运行时解析与 kiosk >500kB 警告。双屏 / 联机真实运行未做 |
| 2026-09-18 | 正常交互的“生命力”不再靠自主正弦呼吸搬动骨架，也不再把延迟铺到整副身体：骨盆、躯干、头、肩、肘与腿逐帧取当前输入，只有肘后的前臂 / 手保留方向余势并按当前骨长重建；坏关节优先从骨端点恢复，有上一副合法骨架时冻结。正式 `vitality → Director` 顺序静止 200 秒全链 0.0000mm，对照 `untether` 102.9mm；15/30/60/120Hz 均首帧响应并收敛，reset 后下一人不继承历史。落地职责从 vitality 移到全部骨架变换后的 `groundSkeleton()`：普通已落地路径原对象返回、零整架分配；6 种 A 档 × 5 个漂移中间点以及 inverted/radial 局部余势后都回到 y=0。`npm run check`：core 360 / app 678 / parts 247 件、0 错 12 个既有警告；`npm run build` 通过，只有既有 Vite native-config、字体运行时解析与 kiosk >500kB 警告。真人摄像头与最终网格全链扫描未跑 |
| 2026-09-18 | 变化弧线加了一道 app 层的产品门：合法拓扑不再自动等于可公开播放。orb / furball 仍在谱系声明 `radial`，但没有显式叠加时会退回连通人形；参数化声明保留肢体 / 头身比例。观众控件和随机池也不再暴露 radial，显式 `?plan=radial` 仍可用于 look-dev。90 秒弧线用 60Hz 回放守住 42.2s 尚未漂移、42.4s 已开始但目标仍连通；`npm run check`：core 348 / app 674 / parts 247 件、0 错 12 警告。这是止血，不是 radial 完成；hub tether 与真人回归未做 |
| 2026-09-18 | 忒修斯替换 / graft / remorph 的短命视觉几何不再参与主体落地：芯、墨屑、缩放和飞入 offset 只画；`Creature` 为每个交接槽位另给 `assemble()` 一件满尺寸、在插座上的稳定代理，只算 lift、不进渲染桶，未交接部位继续按真实几何落地。真实 `parts.json` 回归扫描全部槽位（含 foot）、replace / crossfade 与 t=0..1 共 21 个阶段；旧 joint t=0.65 会把稳定头部整体抬 33.2mm，修后完整 4×4 矩阵不动。无 parts 的 placeholder 路径同样有限、不泵动。`npm run check`：core 348 / app 670 / parts 247 件、0 错 12 警告。仍欠：不同 old→new 在交接结束时的 AABB 基准差要做连续插值，脱离 / 墨屑自己的几何还要独立守地板；本刀只隔离短命效果，未宣称变化链完成 |
| 2026-09-18 | 多人主身份交接不再只换精化器 / 稳定器 / 生命力：主通道的姿态时钟、整具与逐骨运动量、触地、腿部保持、Auto Framing 分类、横向身份门和中景纵向基线现在在换 id 时一起断开；同一 id 失联后认回还会重置自己的滤波链。会话弧线、演化、忒修斯与 seed 故意继续。横向根与镜头保留交接前已经画出的当前位置，只清速度、滤波和旧锚点，避免清状态反而造出一帧瞬移。`primary-handoff.test.ts` 跑真实 motion / bone energy / ground 首帧，`framing-lateral.test.ts` 守交接当帧相机像素不变与身份滤波清空；`npm run check`（core 348 / app 668 / parts 247 件、0 错 12 警告）已过；真人多人交接未跑 |
| 2026-09-17 | 自动人数多了一条真正的浏览器验收入口 `scripts/people/accept.ts`：同一份单人合成 Y4M 用两个临时 Chrome profile 串行跑 worker 与 `?worker=off`，只在主身份稳定后计量，并要求 `idle → probing → idle`、确认上限始终 1、单一主 id、无伴随身体、探测档明显低于常态档且前后恢复、从首次导航起无重载 / exception / 2 秒停帧；帧率通过窗口是常态中位数 ≥21Hz、探测中位数 9.75–18.75Hz 且 ≤ 前后较低者的 80%，不冒充精确 `30 → 15 → 30`。每场写 `worker.json` / `main.json`，参数、runner commit、dirty、Chrome 与宿主负载先写 `invocation.json`；复用输出目录会先清旧结果，四份文件共享 `runId`，浏览器报告另记实际 URL、页面 DOM 与入口脚本 SHA-256，旧 PASS / 旧 dist 都不能混成新证据。CDP 连接与每条命令都有 10 秒上限，中断会先杀 Chrome 并清临时 profile。脚本不再把过载宿主误报成产品回归：当前 14 逻辑核机器实测 load 约 190（每核 13.5），仓库原有平滑度探针也同时从历史 58.5–58.7fps 掉到 27.8–35.3fps，因此新入口会在开 Chrome 前以 exit 2 拒绝这类场次。源码 / CLI 守卫 9 条与 `npm run check`（core 321 / app 632 / parts 247、0 错 12 警告）、build 已过；完整浏览器验收 **Not run：当前宿主负载不合格，须在空闲机器重跑** |
| 2026-09-17 | 首次摄像头启动现在是一个有交易边界的降级：权限拒绝、无设备、模型 / 首次推理超时，甚至 `create()` / `start()` 违约 reject，都会先清理失败实例再自动转 replay；回放 chunk 也造不出时留一个安静的空采集，舞台能进 idle 而不把错误抛到 boot。`cameraOn`、选择页手势和运行日志全改读实际留下的 kind，回放不会倒过来操作物种环。永远不出帧的 stream 的 `loadeddata` 等待有 `CAPTURE.startTimeout` 上限，`stop()` 同时摘掉 `video.srcObject`，预览不再把死 stream 报成“摄像头开着”。`capture-startup.test.ts` 7 条守正常路、failed、create / start / stop 异常、双失败、warning 语义和正式入口接线；真权限拒绝 / 无设备的浏览器路径本轮未重跑 |
| 2026-09-17 | 首屏的 `parts.json` 现在只有 `PartLibrary` 一个所有者：展签的物种数和选择页的 themes + 自有件计数都由它加载后分发，不再各自 fetch；普通进场从三次 185,072B JSON 解析收成一次，坏 `?theme=` 深链也从三次收成一次。生产构建经本地计数代理完整走过展签 → 选择 → 舞台，`/parts/parts.json` 实际 1 请求（改前同页 Resource Timing 3 条），58.7fps / 最大间隔 116.7ms；这是一场功能冒烟，不拿短场性能数作优化结论。选择页仍执行原来的“程序化或确有自有件”上场门。索引缺席时这份投影明确标成非权威：内置 `placeholder` 能让无资产开发照常选到一具身体，但不会拿占位名单否掉手写 `?theme=xeno`。`choose-catalog.test.ts` 6 条守零重复请求、正式入口 / 展签接线、坏条目隔离、离线 resolve、占位 / 手动覆盖和 dev themes 旁路 |
| 2026-09-17 | Q3 的四段归因与正式修复落地：长段不在 `stage.pulse`，也不在 0.5–5.4ms 的 `remorph` 同步段，而是未来 live Mesh 第一次进入真实 PassNode/MRT `stage.render()`（tier 1 render 93.5–96.0ms，延迟显示会把同一长段搬到显示帧；另造相同身体无效，只有同一个 Mesh 对象有效）。`Creature.prepareBuckets()` 现在按正式 genome 建将来会采用的同一批桶，资源队列清空后每显示帧只让一个桶在画外走真实后期；相邻档位同桶不重复画，队尾桶保留到真的被 `pose()` 采用，换观众 / 着色 / 赶不上升档 / render 失败都有撤回与原路径兜底。新测试 `tier-bucket-warm.test.ts` 3 条守对象身份、单桶切片、跨 TTL 保留与取消回收；`warm-wire.test.ts` 新增 3 条守运行时接线。生产构建 3 场固定 `seed=7` / fake camera / 显示节拍：59.6 / 59.6 / 59.7fps，9 个 tier 事件窗零 ≥50ms LoAF；tier 1 最坏帧间隔 33.4 / 33.3 / 16.8ms，tier 2、3 全 16.8ms，零 exception / WebGPU validation error。Ego Lite 的 `?demo=1` 可视冒烟也跑到 30 instances / 36 draws、无启动错误；真摄像头 / 120Hz 外屏 / 30 分钟浸泡未跑 |
| 2026-09-17 | Q3 在修正后的正常显示节拍 + 固定 `seed=7` 口径下重跑：前两次升档 3 场 6/6 命中 74–133ms，同三场 6 次忒修斯替换全无 ≥50ms LoAF，问题收窄到整档升档。按原假说试做未来真实桶 + 后期 RT/MRT `compileAsync`，对象根与整 scene 两条路都触发 WebGPU fragment-output / color-target 校验错误；对象根 3 场仍 6/6 长帧，整 scene 单场还增加额外长任务并走到 governor L3。实验实现、调度、测试与 tuning 已全部撤回，生产运行时保持不变；docs/48 §10.8 保留数字和反证，Q3 下一步改为分段归因，不再扩假预热缓存 |
| 2026-09-17 | 摄像头 P0（docs/48 §10.6）复核为测量口径问题，不改生产生命周期：旧探针在无头 / 真窗口都默认关闭 vsync、解锁帧率，实际把页面推到 128–205fps；干净 HEAD 恢复真实显示节拍后，同一个 `seed=7 / autonomous / fake camera` 本地无头 3/3（58.5–58.7fps）、真窗口 3/3（106.6–112.3fps）完整跑完，线上当前构建另 1/1 旁证。`measure.ts` 默认显示节拍，只有 `UNBOUNDED=1` 才进帧成本压力模式；`VIDEO_FIXTURE` 显式接 y4m。现在开 Chrome 前先写 `invocation.json`，`run.json` 记录 commit / dirty、计划与最终 URL / QUERY、mode、DPR、CPU / 网络节流、冷暖缓存、操作时序、实际服务的带 hash 脚本 URL、Chrome / 实际 fps / 最大间隔；摄像头按 `data-action="camera"` 定位且必须恰好一个，非法的 GOV_AT / 数字参数在开 Chrome 前失败。入口后的顶层重载写 `reload.txt` 并 exit 4，不能把产品重载后的第二次启动算成功；冷缓存 Chrome profile 改放系统临时目录并在退出时清理，不再往仓库遗留 60–80MB/轮。deeplink 与 swap 共用实验参数；新守卫 `packages/app/test/capture-measure.test.ts` 9 条；产品 4 秒帧停顿重载兜底保留 |
| 2026-09-15 | 目录在窄屏上不再一上来就铺满全屏：`main.ts` 挂目录时把 `startOpen` 从「展签在不在」单条件改成再叠一次 `matchMedia('(min-width: 621px)')`，和 `shell/entry.css` 让位那条 `@media (max-width: 620px)` 对齐同一条线（两处互相留了交叉引用注释，改一处得改另一处）。根因：entry.css 只在够宽时才为目录让出右边一栏，可原来的 `startOpen` 只看"是不是展签页"，不看宽度，620px 以下目录因此整片铺在展签上（手机与不少旧笔记本正落在这一档）。无头 Chrome 实测 375 / 620 / 621 / 800px 四档：620 及以下目录收起、"目录 CONTENTS" 一个词不挡内容，621 起恢复原来展开的样子（右栏、六个面一次摆出）；手动点开/点外面收起在四档上都照常，不受这条改动影响（`nav.ts` 的 toggle/click-away/Escape 本就不看 `startOpen`）。`npm run check` 全绿（typecheck、core 303 + app 563 测试、parts 247 件 0 错 12 警告，警告与本次改动无关）。改动只落在 `main.ts` 一处调用点，其余四个 `mountNav()` 调用点（about/making/passport/lineage/rooms）不传 `startOpen`，本来就是收起，未受影响 |
| 2026-09-15 | favicon 从一个抽象小图标（一条横杠 + 一个圆）换成字标本身：九个页面共用同一份内联 SVG data URI（`<link rel="icon">`），左对齐两行 `SEE-ME` / `SEE-U`，字体栈和 `--sb-grotesk` 一致（`ZKMSerendipity, Helvetica Neue, Arial, sans-serif`），字重 600（`--sb-weight-head`），左右各留 4/64 的安全边距，两行整体在 64×64 画布里垂直居中（实测 `getBBox()` 钉住字号 15.26 / 字距 -0.61 / 两条基线 y=31、y=44.7，居中误差 < 0.01px）。底色 `#0e0f12`、字色 `#dfe4ea`——`--sb-paper` / `--sb-ink` 的**根**值（`:root` 里的缺省，不是入口层"首屏纸白"那条覆盖），和原来的图标同一对颜色。无头 Chrome 实测 64/32/16px 三档：64、32px 清楚可读，16px 收缩成一块有节奏的纹理（文字类图标在这个尺寸的物理上限，不是这次没做好）。九个文件（index/about/making/lineage/passport/parts/roster/marks/404）逐一替换同一段 data URI，脚本核对过九处全部命中 |
| 2026-09-14 | 跨页过渡开着（docs/47 §4.3 第二轮）：冻 GPU 画布 + 叠层底色令牌 + 入口挡第一帧 + 共享元素声明 + 门槛与看门狗。有头 Chrome 修根因后 7 轮只剩摄像头舞台离开 / 404 → 展签仍白，加门槛后 2 轮 30 跳 0 白；无头十轮未重跑 |
| 2026-09-14 | 多人入镜（docs/50）：`?people=1\|2\|3`，**默认 1**（推理表 §1.2 没跑，证据之前不改）。身份在 `core/src/people.ts` 按几何跟（预测位置 + 尺度 + 姿态描述子，穷举最优配对、出生憋 0.3s、丢 1s 才死、墓地 3s 认亲、`reacquired` 清状态）；伴随身体共用主身体的桶（`instanceColor` 上色、描边外壳只数主身体），无头实测一具 vs 两具帧间隔在噪声里（p95 12.0 / 10.7ms），draws 36 → 18、tris 175k 不变；预算：忒修斯开着两具、伴随身体在场时描边让位，第三具要 LOD；调速器第 7 级只留主身体；工作台 `/dev/people.html`。取证撞到并修掉三件：伴随身体被切成巨人（取景只看主身体）、开合跳上 40 秒 22 次误交接（尺度只看躯干长）、认回来时插出摊在地上的星形。守卫逐条先红后绿（§9）。舞台上拖动 / 转视角 / 拽零件（docs/52）只写了计划 |
| 2026-09-14 | 换页与导航（docs/47）：每一跳前后实测（无头 Chrome + 照 `vercel.json` 发的本地服务）。**落地**：`index.html` 第一帧按 URL 就是对的底色（冷开 `/` 跳变 240→5，707ms 深底没了）；选择页等舞台第一帧再同文档交棒（198→66）；目录与空位深链干净地址（308 1→0）；字体 / 接触音缓存一天（舞台重载重新验证 13–15→4–6）；悬停预取；回到大厅落在选择页；侧室挂目录；`/dev` 进来处；404 页；自检页出口。**跨页 View Transition 做了、量出整帧纯白、两次归因被推翻，整个关着**（§4.3）。守卫 `test/{transitions,page-exits,hop-cost}.test.ts`，逐条先红后绿 |
| 2026-09-14 | 摄像头"先黑一顿"与"大动作卡"（docs/48）：姿态推理、建图、预热挪进 module worker（`capture/pose-worker.ts`），回放驱动身体直到第一次推理完成，身体吃姿态时钟插值（`capture/pose-clock.ts`）；全页帧调速器（`shell/governor.ts`）六级阶梯全部接在已有开关上；device lost 说一句再直接重载、摄像头断了换回回放并说一句。暖缓存按下之后归因 MediaPipe 的长任务 303+195+231+210ms → 0，大动作 >25ms 帧 29 → 7、p99 20.2 → 13.5ms；代价是暖缓存首次认出人晚约 1.1s（回放照跑）。七个守卫文件逐条先红后绿；worker 里第二个任务 `ModuleFactory not set` 与调速器在不锁帧页面上误判两件事是上机当场撞到、先红后绿修掉的 |
| 2026-09-14 | 乐章边界上不再挂任何事件（忒修斯开着时，docs/44 §10.5 第 28–30 条）：身体层音色跟着 `LINE` 三个数滑（`sound/timbre.ts`，改前 39.62s 一帧跳 100%）；档位下限从乐章序号换成 `TheseusState.tier`，升档在锚点替换前 `tierLead` 4s、离边 ≥ `edgeMargin` 3s，`movementChanged` 的 pulse 只留给 `?theseus=off`；第一件替换不再落在 tier 0 团块上（改前 96/96）。新守卫 `core/test/theseus-tier.test.ts` · `app/test/{sound-line,arc-edges,first-replacement}.test.ts`，逐条先红后绿；无头 Chrome `scratch/evidence/arc-edges-2026-09-14/` |
| 2026-09-14 | 「实·有几何」六条全部落地（docs/42 §8）：`athlete` = Atlas（RobotLocomotion/models@3bd1111，BSD-3，MIT CSAIL）、`wheelleg` = LimX WL_P311D 代 W1（Apache-2.0）、`manipulator` = Stretch 3（Menagerie@8161bba，Apache-2.0），各 10 件，入库 118 / 225 / 189 KB，每件 ≤5000 面；wheelleg / manipulator 的 `joint`（每具 14 实例）压到 1500 面，否则 wheelleg 描边后 298,896 面越 `BUDGET.maxTriangles`（`outline-budget.test.ts` 当场红过），之后最坏 200,840。`harvest.mjs --adopt` 多了可执行的授权门（LICENSE 原文须认作 BSD-3/Apache-2.0/MIT 且与声明一致）、`--family=`、`.gltf` 外挂 `.bin`、一个 link 多份 OBJ。`check:parts` 247 件 0 错 12 警告，新增两条越界：`spine.wheelleg.limx` 0.70×、`hand.wheelleg.limx` 1.58×。渲染 `scratch/evidence/real-machines-2026-09-14/`：athlete 读得出（DRC 代）、wheelleg 正面读得出（侧面代头悬空）、manipulator 件对剪影不对 |
| 2026-09-14 | `/dev/vitality.html` 的「骨盆落后 90.97 mm」查实是**靶场目标悬空 90mm**、被 `vitality` 贴地时整体下挪，水平落后 0.00mm；靶场改为按稳定器契约贴地并把骨盆拆成水平/竖直两个数，core 加贴地甩臂守卫（先红后绿：按手贴地 920.79mm、漏进根部 1.000mm）。`choose.ts` 的 92 行模板串 CSS 搬进 `choose/choose.css`，反引号哨兵测试按它自己的遗嘱删除 |
| 2026-09-14 | 左下角读数改成**数控读数屏**：和左上角监视器同一种东西（同列 `safe×0.5`、同宽 `--sb-see-w`、同圆角 10px、同一圈 `--sb-screen-edge`），自发光深底 `--sb-screen` × `--sb-layer-panel` 0.82，不跟场景翻；全等宽三栏（名字｜数｜单位），小数点用数字宽空格垫齐；底边一条「显示 / 收起」，面板贴底所以开合时键不动（实测 835.1→835.1），收起不跨观众留存；名牌在那 4 秒里抬到读数上方。实测修掉四处：窄屏英文代号被切、收起时键挪 1px、白纸场景屏内线消失、backlit 上边缘只差 1.29（小屏幕同病，一起换了边）。九条新守卫逐条先红后绿 |
| 2026-09-14 | `/about` 的九个身体方案各有各的记号（此前六个共用同一个空心圆，在图上断言它们是同一类）；`?theme=` / `?act=` 按 `?plan=` 的规矩判值并 warn；改动史里七行未来日期按 blame 落回 2026-09-13 |
| 2026-09-13 | `BodyPlanId` 补齐成真实的九个、`bodyPlan` 不再是裸 string、未知方案在 `check:parts` 判**错**；`stub`/`towering` 的条目自带比例不再被预设吞掉 |
| 2026-09-14 | 三个侧室开门：`/parts` `/roster` `/marks` 拿到根 URL，**不进目录**，门是 `/about` 正文里的数字（docs/23 §S9.1） |
| 2026-09-14 | 存档 A 档落地（docs/43 §8）：一次走完的相遇留下一行（序号 / 物种 / 粗到天的日期），`/lineage` 多一个数据源，`/about` 把「不参与」说出来。存储只到接口为止 —— 开通是作品负责人的动作（docs/13 §5.1） |
| 2026-09-13 | 五个展陈面逐条核对：过期数字改掉、弧线与物质进 `/about`、两份审计与那次浸泡进 `/making`、护照多一枚章 |
| 2026-09-13 | 会话弧线落地：四个乐章一条时间轴，导演不再摇骰子（docs/40） |
| 2026-09-13 | 两份审计的落地：物种不再被静默替换、`?gl=off` 接上正式程序、降级阶梯前两级真的有人接（docs/36 D2/D4、docs/39 §2.1） |
| 2026-09-13 | 降级路径审计（`docs/36`）：修掉 dev server 上的 `/demo/index.json`；三处证据栏按实测补注 |
| 2026-09-13 | 30 分钟浸泡首次跑通（docs/37）：地板 33MB 不抬，实例/draws/tris 全程单值 |
| 2026-09-13 | 降级路径审计（docs/36）：12/20 条实跑，D1 现场无对外降级信号 |
| 2026-09-13 | 物种审计（docs/39）：3 个物种运行时不存在，静默变成瓷 |
| 2026-09-13 | 第六层 · 工作声落地；第五层 `reveal` / `ground` 触发点已接，旋钮移交 `tuning.ts` |
| 2026-09-13 | 选择页举手滚动落地后 |
| 2026-09-13 | 网格落地补完：`vitality` 认身体方案 + 团块自己落地 |
| 2026-09-13 | 真人录制进库：`assets/demo/` 不再只有合成数据 |
| 2026-09-13 | 声音四层落地后 |
| 2026-09-13 | 身体好看那条线：脚的挂载 + 镜像法线 + 材质统一 |
| 2026-09-13 | T-17 慢回路服务端落地后 |
| 2026-09-13 | A 档新拓扑 `radial` / `column` 落地、六个条目重新分配之后 |
| 2026-09-13 | 加载态 + 目录 + 全流程走查后 |
| 2026-09-13 | 舞台美学：四套场景 + 地平线/接触阴影/粒子修好之后 |
| 2026-09-12 | B 档身体方案 `mass` 落地后 |
| 2026-09-12 | T-16 kiosk 加固 + 录制页落地后 |
| 2026-09-12 | T-09 Stage 落地后 |

</details>

> **表里的数字是"当时量到的"，不是现状。** 「186 件复检」「191 件 0 错」这类
> 写的是那一次跑出来的结果，是证据；改掉它们就成了伪造记录。
> **现状以命令为准**：`npm run check:parts` 会当场打印当前的件数与主题数，
> `node packages/app/poster/build-data.mjs` 会重新推导名单与组合容量。


## 资产流水线

| 表面 | 状态 | 证据 |
|---|---|---|
| Rodin 客户端（提交/轮询/下载/错误码/429/并发闸门） | `stable` | `npm run factory:generate -- --pilot`：6/6 成功，21–47s/件 |
| 幂等台账 `ledger.json` | `stable` | 第二次 `--dry-run` 报 "需要生成 0 个" |
| 预算闸门（单次 ≤40 credits、余额检查） | `stable` | 代码路径已走通；超额分支 `Not run` |
| 规范化（主轴 +Y / socketA 原点 / 长度 1 / 去贴图 / 单 mesh） | `stable` | `npm run factory:normalize`：6/6 无 warning；2.3MB → ~110KB |
| 长轴朝向自动判定（粗端朝下） | `experimental` | 186 件复检：9 个槽位方向一致（少数反例都是 lo≈hi 的 near-symmetric 件，无所谓）。**`foot` 是 12:12 对半分** —— 现在知道为什么了：`endRadii` 问的是「哪一端更粗」，而脚的两端是脚跟和脚尖，谁粗谁细本来就没有定论，所以它必然是硬币。这条对**摆放**已经不再要紧（脚不再按端点挂，见下面「脚长在腿上」那行）；剩下的只是脚**前后可能朝反**这一件事，仍然要逐件 flip，**不花 credits**（T-13） |
| 容差焊接 + 逐级减面兜底 | `stable` | 对 Rodin 返回的 1.5M 面未焊接网格：1,515,338 → 4,884 tris，文件 66 MB → 148 KB |
| 部件契约检查 `npm run check:parts` | `stable` | **208 件 0 错 17 警告**（2026-09-13 当场跑出来的数）。捕获过一次「prune 没跑导致 66 MB」的真实回归。2026-09-13 加了五条**物种级**检查（`docs/39`）：uniform 槽位的 girth 带（docs/26 §H，中位数每次现算，不查 foot —— 脚的长轴走 `axisLength`，girth 被除干净了）、`stub`/`towering` 会丢掉条目自己的比例、零自有件的条目会被 `makeGenome` **静默换成别的物种**、`coverage:'light'` 承诺的 6 个标志性槽位还剩几个、`clearance` 非 `own` 的条目已经进了 parts.json。**五条全是警告**：每一条背后都是一个策展决定（`docs/39 §4`），在人裁定之前把合并门变红只会让人关掉这个门。**2026-09-13 晚：上面那 17 警告里有 2 条已经被修在根上，当场重跑是 208 件 0 错 15 警告**（历史那个 17 不动 —— 它是当时量到的数）。两条是：① `clearance` 门现在有执行者，`buildIndex()` 只写 `ROSTER.filter(isPublic)`，`guest.founder` 不再进 parts.json 的条目表（29 → 28 条目），这条检查降级成第二道闸、应当永远不响；② 「零自有件会被静默换成别的物种」那句话不再成立 —— `makeGenome` 保留物种身份、沿 base 链借件并 warn，选择页也把它挡在轮播外；警告文案改写成"它在画面上只剩配色"，留到 `docs/39 §4` 第 1 条被裁定为止 |
| **部件档案 `/parts`**（2026-09-14 从 `/dev/parts.html` 升成侧室；目录 kind→条目→槽位 · 每条目一栏 · 按槽位分组列件） | `stable` | 接触表升级成可以直接给人看的档案。23 条目 / 191 件全部列出，anchor 图缺失时整列不占位（不出破图），缩略图 IntersectionObserver 懒渲（一个共享 renderer，渲完拷进 2D canvas）、无 WebGL 或单件加载失败退到**比例剪影**。策展三档与"点一下循环 未评→keep→reject"保留，但**展出侧不用色相**（2026-09-14 裁定，docs/26 §F）：未评 = 发丝线、keep = 双线最浓墨、reject = 虚线 + 缩略图四成墨；socket 底实心、顶空心圈；绿/红/琥珀只挂在 `.sb-archive.can-curate` 底下（仅 `/__curate` 在时）。`test/css-tokens.test.ts` 的十六进制守卫顺着根目录展出页的 import 走，`dev/archive.css`、`dev/thumbs.ts` 因此在扫描范围内；`POST /__curate` 探针（无 id 的 POST，中间件在时回 400；**2026-09-14 起改为 `?probe` → 204**，不再每开一次页一条红色 400，docs/47 §5）决定可评级还是**只读**——生产构建下页头如实写「只读」，格子不可点、不发失败请求。截图 `scratch/evidence/ui-parts-archive.png`（dev，可评级）、`ui-parts-archive-readonly.png`（`vite preview` 打的 dist，只读）、改前改后对照 `ui-before-after-parts.png` |
| **工作台出口 `dev/devnav.ts`**（每一个 `dev/*.html` 右上角：「返回工作台」→ `/dev/`、「退出 · 回到作品」→ `/`；`/dev/` 上只有后者；Escape 同向；`?nav=0` 不挂） | `stable` | 2026-09-14。15 页在 1440×900 无头 Chrome 上逐页量：出口框 `[1004,24,1416,44]`，与 HUD（`top:73`）、页头、`capture` 量程统计、任何带字或控件的元素相交 0 处；`record` 侧栏题下让 36px、`accuracy` 的 `#banner` 下让 34px。Escape：figure→`/dev/`、`/dev/`→`/`，在 `record` 的输入框里不走；两条链接实点跳转成立。没有一页自己占用 Escape，排除名单为空（`dev/devnav-state.ts`）。`test/devnav.test.ts` 扫 `dev/*.html`：拿掉 figure 的挂载 → 红「dev/figure.html 挂了 0 次 devnav.ts」。截图 `scratch/evidence/devnav-2026-09-14/` |
| **页面目录 `/dev/index.html`** | `stable` | 列出全部可见页面，每条一句"它能回答什么问题"，分 作品 / 档案 / 工作台 三组。截图 `scratch/evidence/ui-index.png` |
| **`/dev/*.html` 真的进 dist** | `stable` | 以前 vite 只把根 `index.html` 当入口，`/dev/*` **从来没进过产物** —— 本机好好的，部署上去全 404。现在 `build.rollupOptions.input` 扫 `dev/` 自动收全部 html（新增一页不用改配置）。`npm run build` 后 `dist/dev/` 有 10 个 html，`vite preview` 上 `/dev/parts.html` 实打开 |
| 素材策展 `curation.json` / `factory:curate` | `stable` | 第一遍人工过筛：10 件 reject（digitigrade 全 6 件多物体、wheelleg/autonomous 的 spine、head.softwear.a、joint.patrol.a），0 件 keep —— keep 是审美判断，留给项目负责人 |
| 装配预览 `/dev/figure.html`（合成 A-pose × 运行时 PartLibrary/Creature） | `stable` | 已改为驱动真正的运行时模块；`?theme=&seed=&tier=&plan=&pose=&angle=&still=&debug=1`，porcelain/industrial/coral 截图见 `scratch/evidence/creature-*.png`；它抓到了「头被颈骨压扁」这个真 bug。新增 `?pose=apose\|raise\|crouch\|open`（换一副合成姿态，用来证明因果还在）、`?angle=`（冻结转台，取证图之间才能比较）、`?still=N`（headless 必须，否则 rAF 吊住 `--virtual-time-budget`）|
| **形体并排 `/dev/lineup.html`** | `stable` | 把若干条目同时摆一排，各用各自的 `bodyPlan`，验证 `docs/PRD.md §5` 第 3 条。摆位靠**平移骨架本身**，所以两种渲染器都管用。`?ids=&pose=&tier=&seed=&still=`。截图 `scratch/evidence/lineup-six.png`（orb·furball·manipulator·screenface·xeno·autonomous 六具剪影互不相同）|
| 主题 anchor 渲染 `/dev/anchor.html` | `stable` | 21 个条目的 `_anchor.png` 全部生成，截图 `scratch/evidence/anchors-2026-09-12.png`；`guest.founder` 无 `look` 故无几何、渲染失败是预期 |
| image-to-3D（以 anchor 图为参考） | `stable` | 14 个 light 条目 × 5 件跑完：70/70 成功 0 失败，35 credits。**已知偏差**：bbox 细长的槽位（upperArm/foreArm/shin/thigh）成形好；bbox 近立方的 `joint` / `foot` / 部分 `head` 会把躯干 anchor 的轮廓照抄成"小躯干"—— 需要改 prompt/bbox，花 credits，留给人决定 |
| 186 件部件库（5 个 full × 20 + 16 个 light × 6） | `stable` | `factory:plan` 全部 done；`check:parts` 186 件 0 错。`wheelleg` / `autonomous` 只有 anchor 一件：它们的 `spine.<id>.a` 生成坏了，phase 2 已主动跳过，没烧那 5 credits |

## Core（纯逻辑）

| 表面 | 状态 | 证据 |
|---|---|---|
| `types.ts` 冻结契约 | `stable` | 被 core / factory / app 三处共同引用 |
| `attach.ts` 挂载数学（stretch / uniform 双模式 + `axisLength` / `anchor`） | `stable` | 13 个测试，含 1000 次随机不变式 + 4 种退化输入 + 两种模式 + 新增的长轴覆盖与锚点（`anchor` 缺省 / NaN / 越界都退回老行为；老的 9 条一条没改） |
| `filter.ts`（OneEuro / emaAlpha / rollingMedian） | `stable` | 4 个测试，含帧率无关性与抗离群值 |
| `presence.ts` 生命周期状态机 | `stable` | 3 个测试，含"离开途中回来不清零" |
| `evolution.ts` 演化 | `stable` | 3 个测试，含阈值抖动与首次升档时机 |
| **`palette.ts` 材质统一（次要/点缀色向物种主色收敛）** | `stable` | 6 个测试。护栏那条最重要：同一个 `matte.ash` 挂在瓷身上和挂在异形身上，明度仍然差 0.1 以上 —— 统一材质**不许**把物种统一掉（docs/PRD §5 第 3 条）。自发光材质（`glow.signal`）原样放行，它是信号不是配色 |
| `rng.ts` / `vec.ts` | `stable` | 被上述测试间接覆盖 |
| `slots.ts`（BoneId→Slot、SLOT_WIDTH） | `stable` | 纯表；已被 `app/src/creature/assemble.ts` 实际消费 |
| `genome.ts` 抽取 | `stable` | 同 seed 两次刷新 genome 深度相等（`/dev/figure.html?seed=1234` 实测）|
| **`ground.ts` 网格落地（`lowestPointOf` / `groundLift` / `liftMatrixInPlace` / `MAX_LIFT`）** | `stable` | 纯几何：每件部件的局部 aabb 过它自己的挂载矩阵，取全身最低角，整体抬到 y=0。骨架那一层只保证最低的**脚关节**在 y=0，而脚这个部件绕骨轴长出来 → 底面还在骨轴下方半个脚厚（29 个条目实测沉 3~6cm，最深 `digitigrade` −0.059m）。`packages/core/test/ground.test.ts` 量的是真的 `foot.porcelain.a` 包围盒 + 真的挂载参数；端到端由 `packages/app/test/ground.test.ts` 跑真的 `assemble()` × 七种方案证。替换期间只有稳定身体和交接槽位的独立满尺寸代理能决定主体 lift；芯、墨屑、脱离件和事件缩放不反向搬动整机。每个活跃槽位用自己的 `graftCurve(t)` 在旧 / 新代理最低点间插值；所有短命效果在主体落地后再逐件测 AABB，只抬自己、不穿地。真实 parts 对 17 根骨、全部 13 个 joint cap 与全事件进度的回归已过。消费者：`app/src/creature/assemble.ts` 的 `groundToFloor()`，以及 `mass.ts`（只借 `MAX_LIFT` 这条安全阀） |
| **`vitality.ts` 局部余势 + `bodyplan.ts/groundSkeleton()` 最终收地** | `stable` | live 路径不再用正弦呼吸搬动骨架，也不再延迟整副身体：骨盆、躯干、头、肩、肘与腿取当前帧，只追踪双臂肘后的方向，从当前肘按当前骨长重建；坏关节优先从骨端点恢复，有上一副合法骨架时冻结，不把肢链拉向原点。200 秒静止全链 0mm；15/30/60/120Hz 均首帧响应并在固定时间收敛；reset 后下一人首帧无旧方向。落地规则从 vitality 移到全部骨架变换后的 `groundSkeleton()`，正常已落地人形零分配；漂移 6 种方案 × 5 个中间点都回到 y=0，inverted/radial 的局部余势后再次守住最低点。`arc-still.test.ts` 现在按正式 `vitality → Director` 顺序跑 200 秒四段弧线（0mm），对照 untether 为 102.9mm |
| **`bodyplan.ts` 的 `BODY_PLANS` = 合法方案名单（九个）+ `PLANS_WITHOUT_PARTS`** | `stable` | 名单以前只列七个，而线上真的在跑九个（`mass` / `swarm` 是另一条身体实现，`main.ts` 用字符串比较够它们）；`ThemeDef.bodyPlan` 当时是**裸 string**，于是 `bodyPlan: 'quadrupd'` 类型过、测试全绿、`check:parts` 不响，而 `remapSkeleton` 走 default —— 物种**静默地按人形刚体装配出场**，画面上看不出任何异常。现在三道门：类型（`RosterEntry` / `ThemeDef` 收紧成 `BodyPlanId`，拼错在 `tsc` 就红）、`check:parts` 对 parts.json 判**错**（外部数据那一层）、`app/test/body-plans.test.ts` 守「声明 ⊆ 名单」与「名单 ⊆ 接线」。五次刻意破坏各自看过红：加第十个方案不接线 / 谱系表拼错 / parts.json 拼错 / 拆掉 `main.ts` 那句 `=== 'swarm'` / 退回预设吞比例。改后 `check:parts` **220 件 0 错 12 警告**（原 15 警告，少的 3 条是 step 5 修掉的那一类），core 182 + app 262 绿 |
| **`stub` / `towering` 认条目自己的比例（`withPreset`）** | `stable` | 以前 switch 走 `proportion(sk, PRESETS[kind])`，条目自带的 spec 被整份丢掉，出口那遍又被 `FIXED_PROPORTION` 挡住 —— 实测 `{kind:'stub',head:2.1,limb:0.3,torso:0.9}` 与光写 `'stub'` 的关节坐标一模一样。现在预设与 spec 合并（条目写了的字段赢）。**三个条目的剪影因此改变**（参考站姿骨架盒 高/宽/中心）：`droid` 1.325/0.827/0.663 → 1.370/0.827/0.685；`char.diva` 1.987/1.315/0.994 → 1.815/1.198/0.908；`char.line` 1.325/0.827/0.663 → 1.053/0.534/0.527。只写字符串的 `compact` / `char.paper` 逐位未变 |
| `skeleton.ts` / `stabilize.ts` / `motion.ts` | `spec-only` | 见 `docs/11-TASKS.md` T-02/T-04/T-06 |

## Runtime（浏览器）

| 表面 | 状态 | 证据 |
|---|---|---|
| Vite + `three/webgpu` 渲染栈 | `stable` | `/dev/parts.html` 在 WebGPU 下正常出图 |
| **主程序 `src/main.ts`（整条链收口）** | `experimental` | `/?demo=1&debug=1&theme=patrol&seed=99&tier=2` 装出整具真部件身体：**120fps · CPU 0.8ms · 30 实例 · 89k 三角 · 13 draw · 推理 30Hz**，全部在 `BUDGET` 内（`scratch/evidence/main-chain-patrol.png`）。tier 0 时如设计般是素几何（`main-chain-tier0.png`）。**只在回放数据上跑过，没接过真人** |
| **相遇边界 `main.ts resetEncounter()` + 身体 `reset()`** | `experimental` | 自然离场与成功的 webcam / replay 换源共用一条原子清零：Presence、弧线、姿态时钟、取景分类器/横移、滤波/运动/演化、忒修斯与在途换件、慢回路 epoch、存档 era、多人身份/探测、团块能量/表面时钟、点场 0.5 秒拖影全部归零；调速器、已编译 GPU 资源与 slow / visit 的永久失败闸保留。新采集 `create` / `start` 失败时旧场不动；旧/新采集的 `stop()` 抛错被吸收。会话 seed 不再读 `Math.random()`；`?seed=` 跨场可复现，未指定时只在页面入口取一次 crypto 熵，之后由注入 `Rng` 续种。`test/encounter-reset.test.ts` 5 条（含团块/点场 reset 后同对象复用）与相关 51 条定向测试通过；**还没用真摄像头做 replay → webcam → 拔线 → replay 的整条实测** |
| **Auto Framing 冷却提交守卫** | `stable`（逻辑）/ `experimental`（真人观感） | 漏桶在 1.2 秒冷却期间照常积累以保留抗抖历史，但 `full → upper` 与 `upper → stepping-back` 真正提交的那一帧必须仍有当前候选证据；修掉“腿刚回到画内 / 退后候选刚消失，冷却恰好归零，于是用过期桶误切”的交叉边界。两条 30Hz 反证时间线在旧逻辑下 **18 pass / 2 fail**，修复后 `autoframe.test.ts` **20/20 pass**；未改任何 tuning、原生 `faceFraming` 或镜头参数，**仍欠真人近距离左右/上下与远距离稳定性验证** |
| **Auto Framing 横向连续流** | `stable`（接线）/ `experimental`（真人观感） | 模式分类与小屏只读摄像头 30Hz 的未停滞原话，横向跟随改读 `poseClock` 的插值/短外推流，避免 120Hz 渲染下同一推理结果被重复三帧后再跳；时钟进入 `holding` 后，分类、小屏与读数改报无当前测量，不会无限粘住缓存骨架。源码接线守卫会阻止退回 `live`；同一条 30Hz 移动轨迹分别在 60/120Hz 积分，共同时刻最坏偏差 **4.47mm**（门限 5mm）。**仍未做真人近/远距离观感验证** |
| **Auto Framing 单人近/远横向稳定器** | `stable`（逻辑）/ `experimental`（真人观感） | 横向死区从固定米数搬到透视投影**之前**的画面空间；中心与尺度分别去抖后才做 `center / scale`，米制弹簧只管连续性、前馈、限速和舞台余量。合成 30Hz 反证（这里的 `s` 是 `PersonSpec.s`）：近处中景 `s=1.0` 持续横移 1% 最终响应 **10.9mm**（旧实现 0）；远处 `s=0.18` 的中心 ±1% + 尺度 ±20% 反相噪声，尾段摆幅 **7.3cm**（旧实现 19.5cm）；同一舞台横向位置从 `s=0.10 → 0.20` 缓慢靠近，横漂 **7.8cm**（门限 8cm）；远处真实横移最终仍到 **1.01m**。画面↔舞台逆投影 round-trip、近/远收敛不靠 room 饱和、镜像、侧边冻结与每 16ms 连续性守卫都通过。2026-09-18 再把 Webcam 已接受推理帧的 intrinsic aspect 贯通分类、横向、多人身份/站位、小屏与读数；不再把 4:3 和 9:16 当 16:9，且避免旧 pose 误配换分辨率后的新 aspect。同日修正近处裁胯边界：有限但低置信的胯点不再驱动根，改用成对可信肩；画外低置信坐标仍可报左右，但不追外推位置。**阈值仍是合成数据平衡值，欠真人摄像头定标** |
| **Auto Framing 远处身份稳定器** | `stable`（逻辑）/ `experimental`（真人观感） | 换人待确认从“只看 x”改成“x + 躯干尺度”同组连续成立 0.5 秒；出画、坏光、跟丢或系统取景兜底会打断计时。横向尺度跳变现在必须同时越过 **35% 对称相对差 + 0.05 画面高绝对差**：`lateralEvidence().scale = 0.10 ↔ 0.14` 的远距小尺度噪声不再把同一人逐帧拦成 `hold-jump`，带噪持续横移两秒为无噪参照的 **101%**、120 帧 0 次 `hold-jump`；门限外 `0.30 ↔ 0.45` 交替 180 帧全部 `hold-jump`、漂移 0，`0.30 ↔ 0.80` 极端两身份测试也保留。**0.05 与 0.5 秒仍欠远距离真人验证** |
| **玩法扩展点 `src/acts/`（Act / Director）** | `experimental` | follow / echo / resist / facing 是同一条连续线。echo 已从“整副身体迟到 1.2s”改成实时主体 + 前臂 / 手方向余波，历史最多占 0.5；resist 同样只阻尼双臂方向。200mm 手腕阶跃当帧回应 102.2mm，15 / 30 / 60 / 120Hz 都在首帧超过 1mm；骨盆、躯干、肘、腿逐帧取当前映射，骨端从关节重建。HUD 显示目标取样距离 / 实际历史占比。`LineSampler` 由每个 Director 实例持有；整场 reset、主身份换手 / reacquire、进入或退出 untether 都清历史，两个 Director 交错运行互不污染；Act 脱离 Director 时逐字回 live。有跨帧状态的 Act 通过 `instantiate()` 取得 Director 私有闭包：`untether` 的进入姿态 / 相位不再跨双屏串位，艺术参数集中在 `UNTETHER` tuning，普通无状态 Act 不多分配。出错隔离（连续 3 次抛异常自动禁用并回落 follow）的分支 `Not run`；双屏 / 联机真机 `Not run`。**选角已经不是随机加权了**，见下一行 |
| **会话弧线 `core/src/arc.ts` + `tuning.ts` 的 `ARC` 块（docs/40）** | `experimental` | 一条时间轴四个乐章（跟随 40s → 回声 45s → 抵抗 50s → 朝向 45s，比例 `ARC.beats`，`?arc=<秒>` 当场覆盖），导演按它排座次；分档跟着它走（运动量只能**提前**升档，不能把不动的人卡在 tier 0）；物种自己的 `bodyPlan` 推迟到第 III 乐章，用 `blendSkeletons` 从第 III 乐章开头漂到第 III 个地名（85 → 110 秒；原来的 `ARC.crossfade` 4 秒已随 docs/44 §6 第二步删掉，四个玩法现在是 `core/src/line.ts` 一条连续映射上的四个地名）；人走满 `ARC.resetAfter` 8 秒这一场归零。`npm run check` 全绿：core **178 pass**、app **217 pass**、`check:parts` 208 件 0 错 2 警告。新断言 40 条（`core/test/arc.test.ts` 16 · `app/test/director-arc.test.ts` 7 · `app/test/arc-still.test.ts` 7 · `app/test/hud-arc.test.ts` 6 · `core/test/bodyplan.test.ts` +3 · `app/test/flags.test.ts` +1）。**「永远不脱钩」是量出来的**（docs/40 §1）：喂一副一动不动的骨架，四个乐章各站 20 秒、整条弧线走 200 秒，身体在同一段映射之内自己动了 **0.0000mm**；同一套夹具下对照组 `untether` 动 **102.9mm** —— 没有那个对照组，上面那些 0 在夹具没跑起来时也会是绿的（P21）。变异检验三发三中：导演改回随机加权 → 14 中 **5 红**；弧线不看在场（时钟不停、永不归零）→ 16 中 **2 红**；把第 IV 乐章换成脱钩的 `untether` → 30 中 **8 红**；三次还原后回到全绿。**浏览器里也跑通了**（自己起的 5411 端口，跑完即关；读的是页面自己的 `?debug=1` HUD，不是注入的 rAF 计数器）：`/?demo=1&debug=1&theme=porcelain&arc=20` 四段依次走完并停住 —— HUD 的 `arc` 行 `II 回声 62% →III 2s` → `III 抵抗 83% →IV 1s` → `IV 朝向 100% 保持`（停住那一行按设计变琥珀色），`act` 行同帧跟着走 echo → resist → facing，**79–89 fps / CPU 1.8–3.4ms / 30 实例 / 87k 面 / 18 draw**。`theme=xeno`（`inverted`）：第 I / II 乐章**是人形**（tier 0 团块 2.0k 面 1 draw），第 III 乐章整具翻过来，74–85 fps / 1.5ms。`theme=field`（B 档 `swarm`）：第 III 乐章 31% 那一帧拍到了**两具身体同时在场**（人形团块 + 正在淡入的点场），第 IV 乐章只剩点场，70–85 fps / 2.1–2.4ms。三次会话零 console error。**仍然没接过真人**（全部是 `?demo=1` 回放），第 III 乐章漂移的中间帧只有 `field` 那一张，A 档拓扑漂移的中途没有截图 |
| **忒修斯之船的排期器 `core/src/theseus.ts` + `tuning.ts` 的 `THESEUS` 块（docs/44 §2/§3/§9）** | `experimental` | 纯排期器：进 = 弧线秒数 + 在不在场 + 逐骨运动量 + **整具运动量** + 种子，出 = **这一帧换不换、换哪一件**。三分钟里按 `perBeat [2,5,9,10]` 共 26 件（宽限 20 秒、`minGap` 1.2 秒、并发上限 3 = docs/05 §3 那个 3），槽位按 `staleness³ × quietBias × motionBias` 抽。单件事件走 `creature.replace()` 的即时事务，升档 / 慢回路仍复用 `remorph()` / `graft()`；三条路径共享交接容量与落地代理，不共享隐式位移。**「三分钟后一件原件都不剩」是量出来的**：200 个种子 × 180 秒，最差的一场也是 **18/18**。⚠️ 光靠 §9 的 `stalenessPower = 3` **达不到**：实测 200 个种子里有 6 个会漏一件（35 / 54 / 56 / 80 / 121 / 157），所以 `pick()` 里加了一条**收尾保留**（剩余次数 = 还没被换过的槽位数时，只在没换过的里面抽），平均每场生效 0.07 次 —— 另一条路是把 `stalenessPower` 调到 5，但那是拿整条线的随机性去买最后那一次的确定性。`?theseus=off` 整条不建（plan B 是「不存在」，不是「闲着」），`?theseus=<倍率>` 调快慢（宽限不跟着缩）。`?debug=1` 的 HUD 多一行 `theseus 12/18 · 借距 d2 · 下一件 ~3.4s`。新断言 17 条（`core/test/theseus.test.ts` 12 · `app/test/theseus-flag.test.ts` 5），`npm run check` 全绿：core **194 pass** · app **269 pass** · `check:parts` 230 件 0 错 12 警告。**先红后绿七发七中**：拿掉收尾保留 → 最差 17/18；宽限从 0 秒起 → 第一件落在 0.12s；`minGap` 归零 → 两件只隔 0.567s；拿掉并发上限 → 4 件同时在交接；改成「不动就不换」→ 18 变 0；不归零 → 离开 8 秒后仍留着上一场的件；`motionBias` 取负 → 挥手那只手权重变 0；借距喂段内进度 → d3 退回 d0；`makeTheseus` 无视 off → 照样建出了一台。**两条守卫在实测中被发现是瞎的，已重写**：并发那条在默认参数下永远不会响（`minGap` 1.2 = `MORPH.crossfade` 1.2，画面上只可能有一件在交接），现在它把 `minGap` 压到 0.05 秒并叠 `?theseus=10` 才去验；「没人不换」那条原来靠弧线喂时间，而人不在时弧线本来就停表，于是机器坏掉它照样全绿 —— 现在手动把秒数推上去。**§4 借件已接**（`core/src/borrow.ts`）：五个互斥的圈 d0 本物种 / d1 base 链 / d2 同 kind / d3 另一个 kind / d4 慢回路为这个观众生成的件，`borrowCurve` 相邻门槛之间两圈线性混抽；d4 没到货并进 d3；抽中的圈空了先往外退，不退回 d0；策展否掉的件、条目表之外的家族（clearance）、girth 越界件（docs/26 §H，定义在 `core/src/girth.ts`，`check:parts` 读同一份；只留给自己的物种）一件不借。200 场实测均值 I 1.32 → II 1.55 → III 2.10 → IV 2.67，第 III 段起 d0 = 0（分布表与没挺住的五处在 docs/44 §10.5 第 7–11 条）。**§7 替换那一下已接**（`creature.replace()` + `creature/replace-event.ts`）：旧件的芯保持原大到 60%，3 片墨屑（旧件几何缩小、同一个桶）沿骨头散开缩没，新件走 graft 的组装曲线；这一格任何一刻最大的实体 ≥ 0.81 倍（描边不断）。**交接那几帧原来越过帧预算**（正常速率无头 Chrome porcelain 42/40 draw、338k/250k 面），已按 `creature/swap-budget.ts` 修：同时交接件数上限从 draw 预算算（描边 2），忒修斯开着时给替换留一个名额；关节那一格改成一道波（`THESEUS.jointWave` 2，任何一刻两处）；借件多一道往前看一步的预算门。`test/swap-budget.test.ts` 5 条，先红后绿四发四中；归因、代价和没修的（tier 3 跨主题杂交绕过借件门）在 docs/44 §10.5 第 17–27 条。替换当帧开始、不排队，替换音同一帧响。**一个真人都没接过**（只有 `node --test` 和无头 Chrome）。**升档音的挂点已经搬了**（docs/40 §5 第 3 条 / docs/44 §7）：从四个乐章的交接挪到每一次替换，`?theseus=off` 时原样留在交接上；乐章那一段里的每一声都被 `!theseus` 挡着，由一条读 `main.ts` 源码的测试钉住（把挡板去掉 → 红，报出那一行原文）。**整体尺度漂移已接**（docs/44 §5 第 5 条）：方向由会话种子定（三十个种子里两个方向都出现），幅度 smoothstep 到 `THESEUS.scaleDrift = 0.06`，二十秒宽限那一刻只有 **0.2%**，人一走回到 1。`bodyRoot` 的原点是地面所以脚不离地，取景吃的是没缩放过的骨架所以相机不会把它补偿掉。0.06 不在 §9 那张表里（§5 只说做、没给数），它来自取景余量：`FRAMING.heightFactor` 给标准人形留 **0.21 × 身高** 的头顶余量，测试把它压在余量的一半以下（举手的人也不会被切；调到 0.25 → 红）。**浏览器里跑通了**（自建 5411 端口，跑完即关，读的是页面自己的 HUD）：`?demo=1&debug=1&theme=patrol&arc=120&theseus=6&seed=99&nopost=1` 的 HUD 依次是 `0/18 · 借距 d0 · 宽限中` → `5/18 · 借距 d1 · 下一件 ~0.4s · 交接 1` → `18/18 · 借距 d2 · 走完 · 交接 1`，画面上那具 patrol 身上确实混进了别处的蓝色件；**74 fps / CPU 1.4ms / 31 实例 / 186k 面 / 38 draw**，零 console error。`?theseus=off` 那一场 HUD 上**没有 theseus 那一行** —— plan B 是「不存在」，不是「闲着」。尺度漂移只有单测证据：到 overall=0.31 才 1.4%，截图上看不出来，**没有前后对比图**。**动作现在有两个出口了**（docs/44 §3 的裁定）：`motionBiasGain` 决定换**哪一件**（逐骨），新的 `motionRateGain = 0.8` 决定换**多快**（整具 `MotionFeatures.energy`）。0.8 是两个数相除来的：实测一个真在动的人 `energy ≈ 0.3`（静止 0.000 · 单手挥 0.185 · 整具摇摆 0.286 · 双臂大幅挥手 0.774 —— **遥测报的"挥手 ~2"复现不出来，tuning 的 `stillnessSpeedRef = 0.3` 才是对的**），想要的提速是 §2 表里最小的一档 III→IV 的 5.6/4.5 = 1.24 倍，`1 + 0.8 × 0.3 = 1.24`。上限靠**按乐章计的额度**：`lead[k]` 只在第 k 乐章的窗口里累、不早于宽限、段与段之间不结转，所以每段件数仍是 `perBeat[k]`、总数仍是 26，一个一直在蹦的人在第一乐章里最多还是 2 件。新断言 4 条（core 198 → **202 pass**）。**先红后绿五发五中**：改成一个全局额度、不按乐章计 → 第一乐章里换了 11 件（额度 2）且压缩比 0.142（应为 0.806）；加速无视宽限 → 第一件落在 4.02s；把加速写成 `x(1-x)` 的饱和曲线 → energy 0.3→0.774 之后第 2 件从 30.53s 退到 30.78s；`motionRateGain` 归零 → 三条断言的"活性"前置全红（"加速没在这一场里生效，下面的上限断言等于没断言"）；给每个人白送 0.5 秒延迟 → "站着不动的人走的是更慢的速率"。**一个真人都没接过** |
| **全槽位可见脱离 `detachment-policy.ts` + `DetachmentPlan` / receipt** | `stable`（逻辑 / 几何）/ `experimental`（真人观感） | 17 根可见骨段与共享 `joint` 槽位都有命名 profile；首个事件固定由手教语法，末端 / 肢段 / 核心按弧线分阶段和概率开放，随机流与玩法 Rng 隔离。缺省替换、remorph、graft 全部原位；只有通过 renderer 校验、容量与加权预算的 plan 才提交 Genome / `swapped` / 声音。肢段与核心独占全部交接；两脚不并发；joint 每次只点名一个稳定 joint 名；伴随身体不复制。换主只撤位移并保留当前 scale / t；所有 profile 在 15/30/60/120Hz 两端严格回 socket。真实 parts 对所有槽位、13 个 cap 和全进度扫描不穿地、不搬主体；真人幅度、概率、后半程 tether 可读性与声音尚未跑 |
| `src/stage/stage.ts` 舞台（等身相机 / 三点光 / 影子 / **屏幕空间天幕 + 距离雾** / 接触阴影 / 空场粒子 / 升档脉冲） | `experimental` | **地平线那条硬边已经不存在了**（不是调淡了）：改前 `stage-before-idle.png` 第 240→249 行整行平均亮度 9px 内跳 **+5.6/255**；改后 `stage-after-idle.png` 同一带（y=200–284）单调平滑、总变化 **0.26/255**，整张图最大 4px 跳变 2.65/255 且落在页头文字上（量法 `scratch/scan.py`）。做法不是"让两个材质输出同一个颜色"（那条路实测证伪），而是天幕变成一个屏幕空间函数、地面靠 `scene.fogNode` 化进同一个函数。**仍然只在合成 A-pose 上跑过，没接过真人** |
| **舞台场景 `src/stage/scenes.ts`（5 套完整视觉世界，`?scene=` 切换，按物种自动挑）** | `experimental` | `paper` 纸 / `gallery` 白展厅 / `void` 深空 / `tide` 夜潮 / `backlit` 逆光，各一张取证：`scratch/evidence/stage-scene-{gallery,void,tide,backlit}.png`（同 seed 同条目，差别全部来自场景）。同一套场景换物种 `stage-scene-void-xeno.png` 证明**灯的颜色仍归主题、场景只换世界**。切换复用 `lerpLook` 那条交叉淡入（`STAGE.sceneFade = 1.6s`，**不是硬切**），中间帧 `stage-transition-mid.png`。单测 `test/scenes.test.ts` 11 条：五套性格次序、晕心几何下限、雾密度上限、**地面反射不许有中间态**、`applyScene` 不动物种颜色、`pickScene` 分支、未知 `?scene=` 不静默兜底 |
| 接触阴影（脚下那一圈，落点由骨架给） | `experimental` | `framing.ts` 的 `contactPoints()` + `stage.frame(skeleton)` 每帧喂。去重阈值 **0.19m** 是量出来的（脚踝↔脚尖 0.16m、两脚 0.20m，取 0.12 会让每只脚下面出现两个黑圆斑）；离地上限 `STAGE.contactLiftRange = 0.22m` 挡掉手尖（A-pose 手尖离地 0.73m，去重挡不住）。截图 `stage-scene-void.png` / `stage-scene-void-xeno.png` 脚下可见。接触阴影的"地面"取 **y=0**（不是身体自己的最低点）：陷进地里的脚照常有接触阴影，**真跳起来时接触阴影正确消失**（单测两条都钉住）。**脱离时不伪造承重**：`main.ts` 先按 active bone 过滤它覆盖的端点，再把同一份数组交给舞台和 `GroundSense`；rig 单脚 2→1，quadruped 单前肢 4→3，另一只脚 / 其余支点继续。取景始终读完整骨架，不因 cue 过滤而跳镜头；真人与目标投影仍未验证 |
| 地面反射（映天幕，不映身体） | `experimental` | 只有 `tide` / `backlit` 非零，`gallery` / `void` 是 **0**（docs/28 §4：不做中间态）。平面反射 `reflector()` 被否：draw 17 → ~35 而 `BUDGET.maxDrawCalls = 40`；SSR 被否：`?nopost=1` 时整条消失 = 构图变了而不是画质降了。现做法是天幕绕地平线线性翻折 —— 对无穷远的天幕这是**精确**的，零额外 pass。`stage-scene-tide.png`（含 `groundRipple` 涟漪） |
| `src/stage/framing.ts` 按身体包围盒取景（人形严格等身，非人形有限插值） | `stable` | `test/framing.test.ts` 6 条：`PLAN_BOUNDS` 每次跑都和 `remapSkeleton` 的实测值比对；人形画面高度仍是 2.45m；四足/stub/towering/inverted 都不出画。`FRAMING` 的字段**一个没动**，场景的构图票走新增的 `frameLift`（叠加，缺省 0）|
| `src/stage/look.ts` 每个条目一套灯光/后期（`palette` + `axes` 推导，无 23 个 if） | `stable` | `test/look.test.ts` 11 条原样通过（场景字段是**只增**的，`deriveLook` 的老字段一个没改）。`groundFar === bgBottom` 那条断言还在，但它**不再是地平线的成因**——雾才是 |
| **叠在画面上的字翻到哪一侧**（`look.ts` 的 `stageInk()` → `stage.ts` 的 `publishStageInk()` 发 `--sb-on-stage` / `--sb-on-stage-dim` / `--sb-ink-strong`） | `stable` | 输入从 `look.bgBottom` 改成 `look.skyTop`：`bgBottom` 是**身体背后那团晕**（画面最亮的一块，长在正中），而字坐在两个角上。实测 1600×900、五套场景 × 弧线 0/0.35/0.65/1.0、右上角节题（1423,24 153×78）与左下角那一行（48,805 300×47）底下的真实像素 —— 「夜潮」晕 0.592 / 角 0.0017、「逆光」晕 0.603 / 角 0.0013，旧输入在这两套上发深墨，对比度 **1.14–1.36:1**（20 格里错 8 格）。改后 20 格全部翻对，最低 **6.98:1**（白展厅 arc=1 左下角），最高 16.2:1。阈值 0.18 没动 —— 两套墨对比度相等的交点**实算是 0.176**（这里原来写 0.166，喂的是 0.7231 / 0.0106，而 `#dfe4ea` / `#1a1d21` 的亮度是 0.771 / 0.0121；2026-09-14 更正），**错的从来是输入不是那个数**。单测 `test/stage-ink.test.ts` 4 条，把实测的角上亮度写进断言。取证帧 `scratch/evidence/{before,after}-<场景>-arc*.png`。**现在它是兜底**：各角按像素翻的那一份在下一行 |
| **角上的字按底下渲染出来的像素翻墨**（`stage/ink-regions.ts` 纯逻辑 + `stage/ink-sampler.ts` 薄壳，发 `--sb-on-stage[-dim]-{tr,br,bl,tl}` / `--sb-ink-strong-{tr,br,bl,tl}`） | `experimental` | 上一行是**每场一个数**，跟不上从角底下走过的身体与晕、弧线改的灯、中途换的场景。现在舞台 ≤ 2 Hz 把画布缩进 64×36 的格子，按四个角的元素框（右上 `.sb-corner` / 右下 `.sb-exits` + 右下提示 / 左下名牌 / 左上小屏下那句话）量平均相对亮度，各角**各自**翻。**滞回带是推出来的**：两套墨都还过 3:1 的那一段 0.136–0.224（在 (L+0.05) 尺度上以交点 0.176 为中心），带里不翻；越带后要连续 2 次采样都在外面才翻。组件在自己的容器上把全局令牌重指到这一角（`.sb-corner { --sb-on-stage: var(--sb-on-stage-tr) … }`），`type.css` 里各角令牌缺省 = 全局那一份，首屏 `!important` 翻回去；控件面板里有纸底的选项格换回纸上的墨（`--sb-paper-ink[-dim]`）。**读不到像素**（抛异常 / 读到全透明 = WebGPU 画布那一刻不可读 / 拿不到 2D 上下文）→ 撤掉全部各角令牌、退回上一行的场景墨，只 log 一次；连续 8 次失败就不再试。`?gl=off` 不采。**画和读分在两帧**：`drawImage` 必须紧跟 `stage.render()` 里的那次提交（WebGPU 画布出了那个任务就读不到），`getImageData` 挪到下一帧开头的 `tick()` —— 放在同一个任务里读回要等 GPU 画完整帧，无头 Chrome 实测 getImageData 中位 **4.6 ms**、采样帧 **6.7 ms** 对普通帧 1.8 ms，越过 P5 的每帧 4 ms；分开之后 drawImage **0.1 ms**、getImageData 中位 **0.7 ms** / p95 2.4 ms，读回那一帧 2.5 ms（p95 4.5）对普通帧 1.8 ms（p95 2.3）。`getImageData` 每次新建一个 9.2 KB 的 ImageData（平台没有不分配的读法），2 Hz、不在逐帧路径上。单测 `test/ink-regions.test.ts` 6 条 + `test/css-tokens.test.ts` 扩 1 条、改 1 条；**先红后绿**：带收成交点且不驻留 →「带内来回翻了 40 次」；四个角共用一个 tracker → 左上的 strong `'#fff' !== '#000'`；读回失败不撤 →「读不到像素时还留着 --sb-on-stage-tr …（12 个）—— 过期的颜色」；删 `type.css` 里 `--sb-on-stage-tr` 的缺省 →「只在运行时被写入，type.css 里没有缺省值」（**原守卫在这里是瞎的**：first-screen.css 里那一份被当成了缺省值，已改成只认 type.css）；首屏不翻 `-bl` / 右上列不取 `-tr` → 各自点名。⚠️ `INK_SAMPLE_HZ` / `INK_DWELL_SAMPLES` 本该在 `tuning.ts`（冻结契约，已报需要变更契约）。**左上角有什么字**是在舞台页上量出来的（无头 Chrome 列出四个象限里每个可见文本元素）：`?demo=1` 下左上角**没有字**（`?debug=1` 的 HUD 是仪表、自带深底，故意不跟）；摄像头版只有小屏下面那一句 `.sb-see-word`。字标 / `ui/mark` / 入口展签 / 加载态**不在舞台页的左上角**：前两者只在大厅、关于、谱系、护照、做的过程那几页；后两者是整屏 `--sb-paper` 实底，盖住舞台而不是压在舞台上，和读数同类，不跟。**顺带修掉的真 bug**：那句话的中文主行不写颜色，吃 `type.css` 的 `.sb-zh { color: var(--sb-ink) }`，永远是全局浅墨 —— 「纸」1.14:1、「白展厅」1.55:1（改前改后都是，因为那一行根本不读舞台墨）；现在 `.sb-see-word` 连 `--sb-ink[-dim]` 也重指到 `-tl`，守卫先红（"ui/preview.css 没有让 --sb-ink 取 --sb-on-stage-tl"）后绿。**两套墨的物理上限**：摄像头版在空场、身体到场前的中灰台面（顶带亮度 ≈ 0.21）上，两套墨里好的那一套也只有 ≈ 3.1:1，英文细字读得很吃力 —— 这不是采样的错，是两极墨在中灰上的上限。`npm run check`（core 222 · app 381 · parts 230 件 0 错 12 警告）与 `npm run build` 在 rebase 到 `origin/main` 之后全绿。**没接过真人、没在带窗口的真 GPU 上量过帧时间**（只有无头 Chrome）。**前后对比（2026-09-14，无头 Chrome WebGPU，porcelain + `?demo=1`，前 = `?gl=off` 即场景墨，后 = 按像素采样；对比度按渲染任务里读回的画布像素 × 实际计算出的字色算，取整场最坏的一格；左下与左上用与真元素同类名的替身，因为名牌 4 秒就走、`?demo=1` 下小屏不挂）**：1600×900 空角，五套场景 × 四个角，前后最坏值差 ≤ 0.1（纸 15.0–18.6 · 白展厅 6.8–9.6 · 深空 12.5–16.0 · 夜潮 11.7–17.1 · 逆光 13.3–18.2，左上 15.13 / 7.38 / 12.51 / 11.67 / 13.30）；1000×900 + 目录展开（右上一列伸到晕上、右下压着身体）：最坏逐像素值（p10/p90）右上 8.94 / 5.57 / 4.86 / 4.39 / 4.84，右下 11.56 / 5.62 / 13.10 / 14.60 / 15.91。**两轮里没有一个角翻过边**（flips = 0）：角底下的平均亮度在亮场景最低 0.35、暗场景最高 0.075，离带（0.136–0.224）都很远，所以前后选的是同一套墨，差值是两次跑的帧时序。也就是说**这批取证证明了"不退步、不闪、够便宜"，没有证明"跟得上"**：让一个角真正越过阈值的情形（中途换场景、身体整个挡住一角、中灰台面）无头回放里没有拍到。采样代价（同一批，n ≈ 50–70/场）：drawImage 中位 0.1 ms，getImageData 中位 0.6–0.8 ms / p95 ≤ 2.4 ms，读回那一帧中位 2.5–3.2 ms / p95 ≤ 4.5 ms，普通帧中位 1.7–2.1 ms；**不加 draw call**（画面上 `draws 42` 超预算是忒修斯那条线的事，不是这里）|
| `src/stage/post.ts` 轻 bloom / 微 DOF / GTAO / 暗角 / 颗粒（three addons，无新依赖） | `experimental` | 暗角与颗粒改由场景给（`look.vignette` / `look.grain`）：逆光几乎不要暗角（画面本来只有中间亮），白展厅反过来要重暗角否则亮底会一路铺到画框边。降级阶梯第 1 级实拍 `stage-nopost.png`（`?nopost=1` 照常出画，只掉后期）。**踩过的坑**：scene pass 继承渲染器 MSAA 会让 GTAO 的 `textureGather` 无重载、整条 AO 管线静默失效 → `samples:0` + 末尾 FXAA |
| `src/stage/particles.ts` 空场呼吸粒子（相位由累计时间驱动，无循环） | `experimental` | `stage-after-idle.png`：IDLE 不黑屏，而且**这次真的看得见**。改前看不见不是"淡"：0.016m 的公告牌在 1600×900 上只有 ~7px，乘 0.35 基础不透明度后对单像素的贡献不到 1/255，**存不进 8bit**。尺寸提到 `STAGE.particleSize` + 场景倍率（暗场景 1.45–1.8×；`gallery` 明确为 **0**，加性混合在亮底上等于不存在）。对照 `stage-before-idle.png` |
| 升档视觉事件 `stage.pulse(tier)`（600ms / 全身 +8% / 0.15s 内 dt×0.4） | `experimental` | `stage-pulse-before.png` vs `stage-pulse-peak.png`：HUD 现场读数 **全身 +7.1%**（截图那一刻的包络值，峰值 +7.6%），被照亮像素的线性亮度实测 +6.4%；`timeScale` 轨迹 0.40 → 0.47 → 0.67 → 0.87 → 1（0.15s 恢复）。**没接进 main.ts**：停滞要生效，收口时要把 `stage.timeScale` 乘进 creature/act 的 dt |
| `src/assets/library.ts` PartLibrary（parts.json + glb + 程序化占位） | `stable` | 把 `parts.json` 改名 → 页面照跑，30 个占位实例（`creature-fallback-no-partsjson.png`）；单个 glb 改名 → 只有那一个槽位退回占位，16/17 正常（`creature-one-glb-missing-head-fallback.png`）|
| **运行时读 `/parts/curation.json`**（`library.ts` 的 `loadCuration()` → `makeGenome({ rejected })`） | `stable` | 人眼剔掉的件真的不进池：`npm run check` 里 `packages/core/test/genome-curation.test.ts` 拿**真的** `parts.json` × `curation.json` 跑穷举 —— 所有 theme × tier 1/2/3 × 60 seed，10 件 reject 一次都没抽到；同一组里还钉住"没有物种因为策展消失"（可选物种名单加不加 `rejected` 完全相同）。`npm run check:parts` 当场复述这个集合：`策展: keep 0 · reject 10`，其中 3 件仍留在 `parts.json` 里是故意的（docs/14 §5：genome 排除、文件保留）。取不到时**不拖垮资产层**：`loadCuration()` 与 `parts.json` 分开 fetch，失败只是"没有人工品控这一层"，`console.warn` 一行后退成空集合（ADR-4 / P3）|
| `src/creature/assemble.ts` 纯装配（挂载 + 关节盖片） | `stable` | 30 个实例的 `M·(0,0,0)` 与 `bone.p0` 误差 0；stretch 槽位 `M·(0,1,0)` 与 `p1` 误差 0 |
| **A 档新拓扑 `radial`（无躯干）/ `column`（单柱）** | `experimental` | 纯函数，`packages/core/src/bodyplan.ts`。`radial`：四条肢摊成四条绕核心的轨道弧，弦长 = 骨长（部件不被拉伸），半径 = 末端离中心的距离、高度 = 末端相对中心的高度、朝向 = 肩轴；核心压到 0.40。`column`：六块腿骨首尾串成一根桅杆，双臂是顶端分支，蹲下按之字折叠（只改方向不改长度）。`test/bodyplan.test.ts` 新增 10 条，覆盖"真的没有躯干/没有腿"与三条因果（抬手 / 蹲下 / 张开）。取证 `scratch/evidence/plan-radial-*.png`、`plan-column-*.png`。**只在合成骨架上跑过，没接过真人**；2026-09-18 起 radial 仅保留显式 `?plan=radial` 开发预览，自动旅程 / 控件 / 随机池均隔离，column 不受影响 |
| `inverted` 的落地基准修正 | `stable` | 原来按"最低的脚"贴地，而倒过来之后脚在最上面 → 头被按到地板以下。改成 `PLANS_WITHOUT_FEET`（`radial` / `inverted`）按**整体最低点**贴地，且出口的比例遍沿用同一基准。这是 `xeno` 换成 `inverted` 时抓到的 |
| 六个条目重新分配身体方案 | `experimental` | orb/furball→`radial`，manipulator/screenface→`column`，autonomous→`quadruped`，xeno→`inverted`，char.paper→`towering`（`towering` 与 `inverted` 从此不再是零使用）。改的是 `roster.ts` 的 `BODY_PLAN`，跑 `factory:index` 只重写 `parts.json` 的 themes；**191 件 parts 数组逐字节未变**（改前后 JSON 比对），`check:parts` 191 件 0 错。orb / furball 的声明仍在，但 radial 当前不会自动到场；它们的公开旅程回到保留身材的连通人形，等 tether 验收 |

| **脚长在腿上（`foot` 的挂载 + `FOOT` 三个旋钮）** | `stable` | **改之前脚是躺在地上的**：`foot` 按 uniform 挂，长轴尺寸 = `SLOT_WIDTH.foot / localGirth` = **0.39m**，和 0.17m 的脚骨毫无关系；而部件契约把 socketA 放在长轴端点，于是整只脚从脚踝**往前平铺**出去、戳穿地板，脚踝以下是空的。现在脚长由脚骨算（`FOOT.lengthOfBone` 补上踝后面那截脚跟，带钳位），踝钉在脚长三成处（`FOOT.anchor`），`SLOT_WIDTH.foot` 改读「脚宽」0.115。取证 `scratch/evidence/body-feet-before-after.png`（同机位同 seed 的脚部特写）与 `body-{porcelain,xeno,manipulator}-{before,after}.png` |
| 关节盖片盖住接缝 | `experimental` | `MORPH.jointCapScale` 0.75 → 0.95。0.75 时盖片比它要盖的那根骨头还细，肩/胯/膝的穿插照样露在外面 —— 盖了等于没盖。取证同上那六张图。**审美判断，没有量化判据** || `src/creature/body.ts` `BodyInstance` 接口（身体方案的插拔点，docs/18 §3） | `stable` | 纯提取，`creature.ts` 一行没动。编译期断言 `Creature extends BodyInstance` 在 `npm run typecheck` 里（把 `pose` 签名改坏会立刻红）；`mass.ts` 是第二个实现 |
| **`src/creature/mass.ts` 团块身体（B 档 · MarchingCubes metaball）** | `experimental` | `/dev/mass.html` 实测（M4 / Chrome WebGPU，合成 A-pose 17 骨 = **87 球**，`pose()` 连续 200 次）：**res40 = 1.85ms avg / 3.4ms p95 · 2,484 三角 · 1 draw call**。res 阶梯 16/24/32/40/48/64 → 0.27 / 0.61 / 0.89 / 1.85 / 3.97 / 9.64ms，三角 594 / 1152 / 1740 / 2484 / 3340 / 5928，**全部 1 draw**。大动作姿势（92 球）res40 = 2.98ms avg / 6.6ms p95。降级旋钮（`setRes`）实测有效。**CPU 比刚体贵、GPU 比刚体便宜**：同一副骨架下刚体版 `pose()` 0.17ms / 87,844 三角 / 17 draw，团块 1.85ms / 2,484 三角 / 1 draw。截图 `scratch/evidence/mass-still-res40.png`、`mass-big-res40.png`、`mass-lowres-res16.png`、`mass-highres-res64.png`。**只在合成骨架上跑过，没接过真骨架，也还没有任何条目真的用它**（见下） |
| **团块也落地（`mass.ts` `pose()` 第 6 步 + `stats.lift`）** | `stable` | 团块身体**不走 `assemble()`**（它一个槽位件都不实例化），所以网格落地那条路从来没管到它 —— coral / char.dumpling / char.ghost 和每个人的 tier 0 开场形态（`nascent.ts`）一直沉在地板里。量的是 `MarchingCubes.update()` 之后 geometry 里**这一帧真要画的顶点**（不是球心+半径：融合会让表面胖出球半径之外，胖多少不是一个能写下来的数 —— 那是估计不是测量，P21），世界最低点 = `center.y + lift + localY·half`，令其为 0。`packages/app/test/mass-ground.test.ts` 3 条：站姿修前 **−0.0620m**、蹲姿修前 **−0.0467m**，修后两者 \|y\| < 1e-6；第三条钉住"进出场那几帧冻结抬升"（`shrink` 把物质收向质心，那时重算会让团块一边化开一边往地上掉）。测试读 `geometry.drawRange.count` 而不是 `mc.count`，绕开文件头 §3 那个字段名坑。钳位复用 `core/ground.ts` 的 `MAX_LIFT`。**仍欠**：抬升只改渲染网格，喂给 `stage.frame()` 的骨架没同步，所以团块的接触阴影仍落在骨架的落点上（刚体那条路也一样，见「明确还没接上的」）|
| **`src/creature/swarm.ts` 点场身体（B 档 · 跟着活骨架走的公告牌点云）** | `experimental` | 修的是一个**物种自我矛盾**：`field`／「场」的 tagline 是「身体消失，只剩运动」、`source:'procedural'`、自己一件部件都没有，但它**没有 `bodyPlan`** → `main.ts` 算出 `planKind='rig'` → 走刚体装配、沿 base 链向别的物种借满一整套四肢。改前取证 `scratch/evidence/field-before-rig.png`：一具黄色人形机器人，**30 实例 / 28,043 三角 / 18 draw**。改后是一片 1400 点的云：`scratch/evidence/field-after-swarm-{apose,raise,crouch,open,front}.png`（四副姿态证明因果还在）、`field-main-demo-motion-{a,b}.png`（主程序回放里动起来的样子）。实现**不新造渲染器**：复用 `stage/particles.ts` 的呼吸粒子团，只把「聚拢位置」从写死的 tier 0 站姿换成一个 16 格的**运动历史环**（17 骨 × 2 端点 = 34 个 vec3／格，`SWARM.trailStep` = 35ms 定速推进），每个点按自己的延迟（`rand^SWARM.lagCurve`，多数贴着「现在」、少数拖在后面）在环里插值取自己那一刻的位置 —— 于是站住时点叠回同一个姿势（形短暂出现），一动就沿轨迹拉开（形被运动吃掉）。每帧 CPU 只有 34 次 `Vector3.set`（而且只在环推进的那一帧）加逐点量一次最低点，**帧循环里不写大数组**（P5）。成本（真时钟，页面自己的 `?debug=1` HUD，`/?demo=1&theme=field&tier=2&debug=1`）：**draws 1 · tris 2.8k · cpu 1.4ms · instances 0**；同一台机器同一分钟的刚体对照（porcelain）**draws 18 · tris 85k · cpu 2.2ms · instances 30** —— 三个轴都更便宜。⚠ fps 两边都读 60，但这是 headless Chrome（日志里有 `CVDisplayLinkCreateWithCGDisplay failed`，rAF 退到 60Hz 兜底定时器）：**两边都顶在这个上限上，所以这组数只证明「没掉下来」，不证明「不花钱」**；按 `docs/18 §7.5` 它不是真实帧率读数，**没有拿到带窗口的交互式读数**。落地是点云自己量的（`core/swarm.ts` 的 `lowestSwarmY`，**逐点**，不是「骨头最低点减抖动半径」那个包络 —— 按包络落地会让看得见的那片点浮在空中一层抖动半径），`SWARM.liftTau` 平滑，钳位复用 `core/ground.ts` 的 `MAX_LIFT`。`packages/core/test/swarm.test.ts` 10 条（红→绿实测：去掉抖动 2 红、延迟改均匀 1 红、骨长加权改等权 1 红）。**只在合成骨架与回放数据上跑过，没接过真人** |
| `/dev/figure.html` 认得点场 | `stable` | 这一页此前对 `field` 有两个洞：`themeIsUsable` 把它整个滤掉（打开 `?theme=field` 会静默退回列表第一个条目「瓷」——**这一页看不见的那个物种，恰好是问题最大的那个**），而且它只驱动 `createCreature`。现在不实例化部件的方案（`mass` / `swarm`）不再被可用性过滤挡住，`swarm` 由这一页直接渲染，HUD 换成「点 N · 三角 · draw/上限 · 抬升 Nm」并明说「无槽位：整具身体是一片点」。`mass` 仍不在这一页上路由 —— 它有自己的 `/dev/mass.html` |
| 团块 vs 刚体并排对照 | `stable` | `scratch/evidence/mass-vs-rig-compare.png`（`/dev/mass.html?mode=compare&pose=big`）：团块是一具连续的身体，刚体版在同一姿势下读作一堆悬空零件。这张图是「像不像原作那种流过身体的物质」的判断依据 |
| `/dev/mass.html` 团块调试页（合成 A-pose ↔ 大动作 · HUD · 方向键调 res · 三模式） | `stable` | 上述全部数字与截图都出自它。`?mode=mass\|rig\|compare&pose=a\|big\|anim&res=&angle=&still=`；`?still=N` 是 headless 取证用（永不停的 rAF 会把 `--virtual-time-budget` 吊住） |
| 团块的降级路径（`presence` 进出场 / 退化骨架 / 出界） | `experimental` | 浏览器控制台逐条跑过，**全部不 throw**：IDLE 与 LEAVING t=1 → 0 三角 0 draw 且隐藏；ENTERING 0→0.5→1 → 0 / 964 / 2,274 三角（物质向质心收回去）；`null` 骨架、空 bones、全 NaN 关节、confidence=0、height=0、length=0、身体被挪到盒外 50m、`dt=NaN`、`dt=10s` 逐个跑过均正常返回，且 15 帧内恢复正常出图 |
| `src/creature/creature.ts` 实例化渲染 / remorph / graft | `experimental` | 30 实例 · 85–88k 三角 · **18 draw**（左右分桶后比原来多一点，仍远在 `BUDGET.maxDrawCalls` 40 内）· `pose()` 0.06–0.21ms；换装并发上限由着色语言的 draw 预算得出（描边 2，单 pass 3），其余排队。队列现在只在槽位进入 active 时才开始旧→新，未激活槽位持续画旧件，不再直接从最终 genome 偷跑。`graft()` 热插拔 73 帧完成。**只在合成 A-pose 上跑过，没接过真骨架** |
| **左右不再像两种材质（镜像改走预镜像几何）** | `stable` | 左侧肢体原来靠矩阵里的负 X 缩放做。负行列式把左半身的三角形**全部变成背面**，而 `DoubleSide` 会把背面片元的法线取反 —— 左半身于是「从内部被照亮」，比右半身暗一大截。实拍图里「一只手白、一只手深色带斑」根本不是材质问题，是这个。现在由 `library.mirrored()` 给一块 X 取反**且绕序也翻回来**的几何，行列式保持为正。3 条测试守住它（闭合网格的有向体积镜像后仍为正，索引 / 非索引两种几何都测）。取证 `scratch/evidence/body-feet-before-after.png` 的左右对照 |
| **描边 / 平涂着色（`src/creature/shading.ts`）** | `experimental` | `char.line`（线）的全部辨识度是轮廓上那圈粗黑线，而它此前和另外二十几个物种走同一条 PBR 路径 —— **这个物种的身份是缺席的**（docs/12 早已写明：描边是着色属性，不是几何属性，网格载不动它）。现在每个桶多挂一块**反向外壳**（`BackSide` + 沿法线外推 `TOON.outlineMeters`=12mm），填充换成 3 级平涂（`MeshToonNodeMaterial` + 色阶贴图）。**不走屏幕空间边缘检测**：那是一个后期 pass，而后期会被 `?nopost=1` 和降级阶梯第 1 级自动关掉 —— 机器一慢身份就消失。opt-in 由 `SHADING_OF_THEME` 一张表决定（表里只有 `char.line`，其余物种一个像素不变），`?shading=toon|physical` 与控件条「渲染」组的 `O` 键可在**任何**物种上当场热切（实测：porcelain 默认 18 draw / 86k 面，按 O 变 36 draw / 172k 面，不重载）。推挤写在 `positionNode` 而不是 `vertexNode` 上 —— 后者会接管整段顶点计算、丢掉实例矩阵，一整桶外壳叠在原点。实测（`?demo=1&theme=char.line&tier=2&seed=12345&nopost=1`，同一分钟内 A/B，读页面自己的 rAF 帧率）：physical 120fps / 0.9–1.5ms / 18 draw / 88k 面，toon 120fps / 1.0–2.7ms / **36 draw** / 177k 面 —— **两边都顶在垂直同步上限，所以这组数只证明「没掉到 120 以下」，不证明「不花钱」**。36/40 已经贴着 `BUDGET.maxDrawCalls`，`test/outline-budget.test.ts` 守住它（最坏 18 桶，再多 2 个桶就红）。取证 `scratch/evidence/outline-char-line-{on-f60,on-f150,off}.png`、`outline-porcelain-off.png`、对照裁切 `outline-compare-{head,feet,hand,chest}.png`。稳定性用像素差量过：冻结机位下第 60 帧与第 150 帧**除 HUD 文字外逐像素相同**，墨不闪、不脱离剪影。**已知：`head.char.line.a` 的格栅被墨吃掉**（`outline-compare-head.png`），那是资产问题不是着色问题 —— 该件的配方写的是 「extremely simplified rounded form, no surface detail」，长出来却是一团格栅，和 `curation.json` 里已 reject 的「炸开的尖刺团」同类；量过三个判据（拆开的法线数 / 世界平均边长 / 表面积比）都分不开它和脚，所以不为一件坏资产弯渲染器，建议走策展 reject |
| **物质语言：表面跟着弧线走（`stage/look.ts` 的 `arcWeights`/`applyArc` + `creature/surface.ts`，`MATERIAL` 块，docs/41）** | `experimental` | `docs/40` 的四个乐章此前只写在动作和墙上的字里，表面一个字都没说 —— 而 `docs/26 §I` 自己裁过「**身份住在表面**」。现在一个标量 `arc∈[0,1]` 驱动四条**互相重叠的连续权重**（smoothstep / 余弦钟）：quote 把表面压成被引用的数据（无高光、三个角色的明度压向主色、翻拍台平光）→ grow 把粗糙度按角色分开并给一点自体微光（zoë）→ diverge 把次要/点缀色的色相转开（上限 20°，**主色不动**）→ other 让它有自己的物质，并把**墨色**换成它自己的颜色。灯那一半做成 `LookProfile` 的**第三层**（主题→场景→弧线），只缩放已有字段、不加字段、不碰接触阴影、仍吃 bloom 的 0.45 硬顶 —— **没有第二套颜色/灯光系统**。**运行时成本为零**：只重写已建材质的 uniform，不建桶、不换管线；实测 `patrol` 在 arc 关 / 0.00 / 0.35 / 0.62 / 0.95 五种情况下**都是 36 draw / 56,086 三角**，与关掉逐字相同（`porcelain` 36/171,844、`xeno` 36/175,976、`compact` 36/180,940、`coral` 36/171,552）。取证 `scratch/evidence/material-arc-{off,i,ii,iii,iv}-patrol.png`（HUD 上印着四条权重）、`material-ink-i-vs-iv.png`。单测 `test/surface.test.ts` 9 条（全零权重逐位不变 / 1% 步长上不许跳 / 四段各自占优 / 主色不转色相 / 灯不加字段不碰接触阴影）。**没有人调它**：接 `core/arc.ts` 的 `overall` 要改 `main.ts`（另一条线的文件），在那之前现场看到的是"关掉"的样子。**灯那一半没有取证图**（figure 页自带写死的灯，stage 页没有 `?still=`）—— 按 docs/41 §7 算未验证 |
| **手脚的墨不再碎（`TOON.outlineSlotScale`）** | `stable` | 描边翻成全局默认之后，同一个 12mm 外壳在手和脚上不是"变粗的线"而是**碎片**：一块白团上挂着几片脱落的黑渣，没有起笔收笔。判据：**会变粗的是线，碎成片的是坏的**。**没有标量分得开**（同一具瓷实测世界包围球半径：`joint` 0.079–0.093m 最小却最干净、`hand` 0.124m 碎、`foot` 0.140m 碎、`head` 0.155m 干净）—— 区别是**形状**（手脚是全身仅有的两个有分叉、有凹陷的槽位），所以是一张**按槽位看出来的表**（P15），`{hand:0.40, foot:0.40}`，**其余九个槽位乘 1**。0.40 两头都试过：0.60 仍有碎片、0.25 脚上的墨细成一根头发丝。**不做平滑法线** —— 那会把躯干上「形体交叠处墨变厚」一起削平，而那是有方向的线。验收是一张逐像素差图：改动**只落在手（y 425–500）与脚（y 650–700）**，其余为 0，所以"躯干没有变得更匀"不是自评。外壳材质按线宽缓存（两档 = 两条管线），**draw call 一次都没多**。取证 `ink-{before,after}-porcelain.png`、`ink-compare-{hand,foot}.png`、`ink-{before,after}-torso.png`、`ink-after-{xeno,compact,coral}.png`；单测 `test/ink-width.test.ts` 3 条 |
| **墨的轻重起伏（`TOON.outlineWeightVariation` / `outlineWeightFrequency`）** | `experimental` | 作品负责人要描边"像手画的，稍淡或稍稀，在原有基础上调整"。外壳宽 = 槽位宽 × (1 − 0.5·n)，n 是挂在**部件自己几何坐标**（`positionGeometry`）上的低频三正弦 —— 钉在身体上，肢体动墨不流；**只变细不变粗**，所以手脚永远不超过 0.40 那一档。同一块材质里的节点，外壳材质仍按线宽缓存两块，draw 不变。`0` = 走原来那一句着色器（porcelain/patrol 与改前逐像素相同）。近黑像素：porcelain −0.4% · patrol −3.1% · char.line −1.1%。取证 `outline-hand-*`（docs/41 §8）；单测 `test/ink-hand.test.ts` 5 条，每条先红过。**只在 A-pose 静帧上看过，没看过它动起来，也没真人看过** |
| **一具身体读起来是一个物种（材质统一）** | `experimental` | 三个材质角色原来各取一个全局材质、彼此没有关系（白瓷躯干 + 深蓝灰四肢 + 白手白脚 = 装错了零件）。现在次要 / 点缀色向**这个物种自己的主色**收敛（`core/palette.ts` + `PALETTE` 旋钮），明度差按 `valueKeep` 留一部分（全抹平身体就没有体积了），表面响应按 `surfaceMix` 靠拢。物种之间的差别由并排图自证：`scratch/evidence/body-three-species-after.png`（瓷 / 异形 / 机械臂，形体与色调都分得开）。**审美判断，没有量化判据** |
| 摄像头采集 / MediaPipe（`src/capture/webcam.ts`） | `experimental` | 整条路跑通但**没见过真人**：headless Chrome + `--use-fake-device-for-media-stream` 下 GPU delegate 起来、wasm 与两个模型加载、mask 产出、`latest()` 不抛也不阻塞（`scratch/evidence/capture-webcam-fakecam.png`）；权限被拒时不白屏且 `lastError=NotAllowedError`（`capture-permission-denied.png`）。**fps ≥ 30 未验证**（headless 软件渲染只有个位数），U1/U2 未实测。**2026-09-14 推理挪进 worker（docs/48 §3）**：Apple silicon 无头 Chrome + 假摄像头（合成人形 y4m）上 GPU delegate 在 worker 里起来、预热 1104–1274ms、推理 27–31Hz；`start()` 在第一次推理完成后才返回，判失败看 `failed` 不看 `lastError`。主线程那条路保留为降级（`?worker=off`，§2.2 有它的一场）。**2026-09-18 分割器从持续旁路改成按需单张**：启动不解析/下载/建 ImageSegmenter；慢回路武装才请求，同一 worker 初始化失败不重试；每位观众由 generation 隔离且成功后停，取消会 close 缓存 bitmap，worker 每次回调 finally 释放 MediaPipe result。主线程降级明确不建第二张图，优先保住单人姿态。纯状态/接线/类型检查已过，**真浏览器的启动字节、建图耗时、长期内存尚未重测** |
| **姿态推理 worker（`capture/pose-worker.ts` + `pose-protocol.ts`）** | `experimental` | 一次一帧（`VideoFrame` 转移）、worker 常驻、悬停「摄像头」那一行即预取；worker 死了重拿 2 次、一帧 2s 不回就退休这个 worker，走同一条有上限的重拿，不向已堵塞的队列每 2s 多塞一帧。每帧仍用 stamp 保有所有权：跨 Capture 的旧 pose / fail 不得释放或写入新帧，异步 `createImageBitmap` 迟到也不再发旧帧。暖缓存按下之后归因 MediaPipe 的长任务 4 次 939ms → 0（docs/48 §2.1）。上机撞到 module worker 里第二个 `createFromOptions` 一律 `ModuleFactory not set`（抠图起不来、GPU→CPU 回落同样会起不来），修为每次创建前装回工厂，`test/pose-worker-factory.test.ts`；修后三场控制台无抠图告警。**2026-09-18 运行中换 `numPoses`**：协议带 request id 和显式成败回执；worker 与主线程降级共用有 deadline 的 applied / desired / in-flight owner，等待中只留最后目标，只有匹配成功才推进 applied。普通 reject 释放互斥、继续用旧图；超过 2500ms 不会只放开 busy，而是终止并清掉 worker，或把主线程 landmarker 隔离到旧 mutation 真 settle 后才 close，当前 Capture 标 lost 后由外层回到 replay。迟到回执没有任何状态权；在途时两条路径都不调用 `detectForVideo()`。`reconfigure.test.ts`、`people-reconfigure.test.ts`、`cadence.test.ts` 与 `frame-flight.test.ts` 覆盖状态机、资源隔离、重建 / 推理互斥和帧所有权。**没有真人、真摄像头、真显示器**；Safari / Firefox 的 worker + OffscreenCanvas WebGL 路径没跑过；**真摄像头连续 1→3→1 与真 MediaPipe 慢挂未测，2500ms 不是硬件调优结果** |
| **姿态时钟（`capture/pose-clock.ts`）** | `stable`（逻辑）/ `experimental`（观感） | 两次推理之间插值（延迟 = 测到的推理间隔，封顶 70ms）、外推 ≤60ms、停滞 250ms 保持 600ms 后交出 null、"没人"立刻 null、隔一大段不插值。身体吃它的连续流；小屏、读数和取景吃未停滞的采集原话，进入 `holding` 后不再把 `capture.latest()` 的旧骨架报成当前测量。`test/pose-clock.test.ts` 9 条（含 30Hz 推理 × 120Hz 渲染步长均匀与六状态测量表）。**观感没有真人看过** |
| **多人推理时钟（`capture/inference-clock.ts`）** | `stable`（逻辑）/ `experimental`（真人多人） | 多人 tracker 与自动人数确认只在 `inferredAt` 变大时推进，并按相邻推理时刻算 `dt`；旧 Capture 退到 `RawPose.t`，重复/倒退/NaN 时刻静默忽略。反证：同一份结果在 120Hz 被连读 3 秒，轨迹仍是 tentative、探测仍是 level 1；换成真实 30Hz 新结果后才分别按既有 0.3s / 1.2s 门限推进。`reacquired` 也只在那份新推理上清一次主/伴随身体的时间状态，不再被 120Hz 缓存帧连续重置。主线一帧只读一次 `latest()`；未改任何人数 tuning。**仍欠真摄像头多人升档验证** |
| **自动人数调度（`packages/core/src/people-probe.ts` + `capture/{people-probe-runtime,cadence}.ts` + `main.ts` + `?people=`）** | `stable`（逻辑）/ `experimental`（真人多人） | 网页与 kiosk 都默认 `auto`，显式 `?people=1\|2\|3` 才固定；控件可交还自动。真摄像头先用单人档；空场不开三人图，只在 tracker 确认至少一名当帧可见观众后才走 3 秒稳定期。之后只有页面可见、未降级、调速器在 L0、帧耗时低于卡顿线时才开 `hardMax` 发现窗，并在窗内把 worker / 主线程降级两条推理路径都降到 15Hz。卡顿或所有人离开会立即收回已确认档并完整退避；闲置时预算不足只暂停倒计时。一扇窗可 1→3；只数 `missing===0` 的当前可见轨迹；输出顺序变化时主身体仍按 tracker id，单人快速横移失配且检测器确实只返回一人时才受限回退 `latest`。回放不参与；mass / swarm 不显示无效人数控件；桶按 3 人预留但身体只在确认后增加。稳定单人不再逐帧上传未变化的全白 `instanceColor`，完整人数快照只随新推理读取，伴随身体管线的 360 帧夹具复用同一个空结果，不再逐 rAF 建多人容器。自动探测在 idle 且无提示时也只随新推理走；一旦开窗仍逐渲染帧检查卡顿，提示仍按 UI 时钟收起。最终身体数仍受 250k 面预算限制。纯状态机、runtime、cadence、instance-color、推理时钟与单人快路都有守卫；**常数、运行中 1→3→1 的 MediaPipe 延迟、远近与遮挡下的真人稳定性均未实测** |
| **帧调速器（`shell/governor.ts` + `governor-wire.ts` + `long-tasks.ts`）** | `experimental` | 七级：墨色采样 → 替换延后 → 推理 15Hz → DPR 1 → 后期临时挂起 → 读数停刷 → 只留主身体，每一级接已有开关（登记表 + 测试核对 `main.ts`）；过载憋 1s 放、余量憋 8s 拿回、至少隔 2s、变化后窗口清空、复发翻倍。HUD `gov=L…`，放下后期时右下角说一次「降级渲染」。`test/governor.test.ts` 16 条 + `governor-wire.test.ts` 4 条先红后绿。第一次上机在不锁帧页面上 ~400fps 把节拍量成 1ms、六级全放 → 加节拍下限 1000/240ms 与 25ms 绝对丢帧下限后，暖缓存那一场只在 worker 建图时放下又拿回第 1 级。**真显示器（60Hz 外接 / 120Hz ProMotion）上没看过它走到几级**。**2026-09-14 第二轮（docs/48 §10）**：强制拨到当时的 L4 实测关后期那一帧 273ms + 367ms（直出管线第一次被用，`NodeBuilder.build` 61ms）→ 空闲里 `compileAsync` 预编译直出（`stage/warm-plan.ts` 决定时机，`warm-plan.test.ts` / `warm-wire.test.ts` 先红后绿）；左侧肢体镜像副本改在件到货后的空闲切片里做（`assets/idle-queue.ts`，`idle-queue.test.ts`）。**2026-09-17 Q3（docs/48 §10.8–§10.9）**：`compileAsync` 对后期 RT/MRT 不等价；改为同一个未来 live Mesh 每帧一桶走真实后期 render 后，3 场 9 个 tier 窗零 ≥50ms LoAF。**2026-09-17 Q2（§10.10–§10.11）**：调速器后期开关从永久 `setPost` 拆成临时 `setPostSuspended`，永久关闭照旧释放；普通 / DPR=3 各 3 场共 6 次恢复全部 16.8ms、零 LoAF、零异常。放下窗归因为真实直出首用；阶梯改成 DPR-first 后，L4 3/3 为 16.8ms / 零 LoAF，持续过载的 L5 仍有 70–73ms LoAF，未消根 |
| **WebGPU device lost / 摄像头断开（`shell/degrade.ts` `degradeTo` / `deviceLostAction` + `main.ts`）** | `experimental` | device lost：右下角「出了点问题，正在恢复」1.5s → 直接重载（跳过前两级，仍吃每会话 2 次的闸）；`destroyed` 不管。`test/device-lost.test.ts` 3 条先红后绿。**没有真的丢过 device**。摄像头 track `ended`：1s 内换回回放 + 「摄像头断开了，换回录像」——**没实拔过** |
| **`/dev/accuracy.html` 精度基准台** | `experimental` | 三路并排跑同一份输入（A 原始 / B 现有=`main.ts` 今天的 / C 新=分组 One-Euro+遮挡补全+质量兜底+最小折叠角），逐关节 visibility/世界坐标/1 秒抖动 σ、逐骨长变异 σ/μ、相对 A 的跟随偏差，外加常驻的**"无法测量"清单**。「⇪ 载入片段」直接读 `/demo/` 离线重跑（不走 rAF —— 页面不可见时 rAF 被掐到 1Hz，实时数字会全是 0，那不是"很稳"是没有数据）。合成片段上的实测数字与读法见 `docs/24 §5`。**它证明的第一件事是"现有输入测不了精度"**：合成片段 z 恒为 0、无噪声、左前臂长度变异 65.7%（人体做不到）|
| **`packages/core/src/refine.ts` 姿态精修** | `experimental` | 分组 One-Euro（基线 = MediaPipe 自己的 world 档 `0.1/40/1.0`）+ visibility 低通 + 遮挡时序补全（20 帧缺口上限，超时放手把 visibility 归零）+ 质量兜底（score 低时压低截止频率）+ `clampFold()` 最小折叠角。9 条单测随 `npm run check` 跑（降噪、遮挡保持、超时放手、兜底斜坡、抗 NaN、不改写输入、骨长守恒）。**没接进 `main.ts`**（那个文件这次不许碰），所以现场行为一点没变；要接需要先把参数提进 `tuning.ts` —— 那是冻结契约，见 `docs/24 §0` |
| **`?model=lite\|full\|heavy` 换档**（`shell/kiosk.ts` + `capture/webcam.ts`）| `experimental` | 三个档位的模型 URL 已就位，认不出来的值退成 null 而不是静默变 lite；采集端把实际档位暴露成 `.model`，`/dev/accuracy.html` 显示出来。**只验到 lite 这条路真的加载过**；full/heavy 没有在真人输入上跑过（`docs/24 §3` 有官方 model card 的 45mm→39mm→36mm） |
| **`?cam=<序号\|deviceId>` 选摄像头**（`shell/kiosk.ts` + `capture/camera-select.ts` + `capture/webcam.ts`）| `experimental` | 在这之前 `getUserMedia` 不带 `deviceId`，现场"外接对着观众、内置对着墙"两台里用哪台**完全由浏览器决定**，操作员唯一的办法是拔线或改系统设置。现在两种写法：`?cam=1` 按 videoinput 序号（人站在现场一台台试用的），`?cam=<deviceId 或唯一前缀>` 钉死一台（开机脚本用的、唯一可复现的那种）。认不出来的值（`?cam=1.5` / `?cam=-1`）退成 null 而不是静默挑一台（和 `?scene=` / `?shading=` 同一条规矩）。**设备表只在 stream 已经开起来之后才枚举**：`enumerateDevices()` 本身不弹权限框，但权限之前它不给 label，而为了凑一张表提前要权限会正好毁掉入口层存在的理由（`shell/entry.ts` 文件头）；所以 `?debug=1` 的 HUD 多一行 `cam <label>`，控制台另打一次全表（每行都写着 `?cam=<序号>` 和 `?cam=<id 前缀>`，直接能抄进 URL）。**要的那台不在时大声回落**：照常开默认那台（`latest()` 仍不抛，文件头第 2 条），但 HUD 那一行变红并写「用的不是你要的那台」——判据取自 `track.getSettings().deviceId`（现实）而不是我们请求的那个 id（意图），否则拔掉外接之后仪表会显示得和一切正常时一模一样（P21）。证据：`packages/app/test/camera-select.test.ts` 13 个用例随 `npm run check` 跑 —— `npm run test -w @smu/app` 由 **110 → 123 pass / 0 fail**，`npm run check` 整条绿（core 146 pass、`check:parts` 208 件 0 错）。红-绿都验过：把 `describeCamera()` 改回"报我们请求的那个 id"（静默回落那种实现）→ **13 中 3 红**（外接被拔掉 / 序号错位 / 拿不到实际 deviceId 三条）；把 `cam` 退成 `q.get('cam')` 原样透传（不校验）→ **13 中 1 红**。两次都 `git checkout --` 还原后回到 13 绿。**没有在真有两台摄像头的机器上跑过** —— 序号顺序、`deviceId: {exact}` 被拒时的实际错误码、以及 Safari 的 deviceId 稳定性都还是纸面的 |
| `/dev/capture.html` 调试页（33 点叠加 + fps/推理 Hz/置信度 + world xyz 量程/抖动） | `stable` | `scratch/evidence/capture-replay-demo.png`：33 点在位，fps 59 / 推理 31Hz |
| `?demo=1` 回放（`src/capture/replay.ts`） | `experimental` | **2026-09-13 降级审计修掉一条 dev-only 的坑（docs/36 §D3）：`GET /demo/index.json` 在 dev server 上返回 200 + `index.html`（Vite 的 html 中间件把路径里的 `index.*` 认成页面；同目录 `pose-*.json` 正常），`fetchClipIndex()` 于是拿到空表，`?demo=1` 一直静默退回写死的 `pose-synthetic.json` —— 即 dev 上放的是 2 秒合成假数据，而自检页同时报「一条片段都没有 ✗」。`vite preview` 与 Vercel 上这个文件一直是好的。修法：`packages/app/vite.config.ts` 的 `demoIndex()` 加 `configureServer` 自己发这个文件。复验：dev 上 `content-type: application/json`，自检页由 ✗ 转 ✓（两段真录制），与产物一致。** 与 `WebcamCapture` 同接口、可直接互换，`capture-replay-demo.png` 是它在播。片段由 `/demo/index.json` 列出、`?clip=<name>` 指定（三种写法 + "真录制永远排在合成数据前"有单测：`packages/app/test/clip.test.ts`，随 `npm run check` 跑）。**2026-09-13：库里有真录制了。** `pose-jumpingjacks`（45.2s，100% 帧有人）与 `pose-walkturn`（20.3s，94.6% 帧有人）都是真人视频跑 MediaPipe 得到的，来源与质量报告在 `assets/demo/SOURCES.md`；左前臂骨长 CV 9.2% / 11.1%（合成数据是 65.7%）。`/demo/index.json` 实测顺序 `pose-jumpingjacks` → `pose-walkturn` → `pose-synthetic`，**`?demo=1` 默认已经挑不到假数据**。仍是 `experimental`：这两段是别人拍的素材，不是"我们的机位、我们的灯光下的一个人"，逆光那一档仍未覆盖（U3） |
| **`/dev/record.html` 真人 pose 录制页（T-16）** | `experimental` | 页面 UI 完整、六段录制脚本（走进→站定→挥手→蹲下→转身→走出）、定速 30Hz 采样、没人的帧记成 `world: []`（"走出画面"在数据里就长这样）。写回中间件 `/__demo` 五条路都用 curl 当场验过：正常写入 → `assets/demo/pose-writetest.json` + 自动重建 `index.json` 且真录制排到了合成数据前面（测试文件已删）；`synthetic:true` / 整段没人 / 坏片段名（`../evil`）/ GET 四条都被拒。摄像头分支只验到**报错不白屏**：浏览器面板里 `NotAllowedError: Permission denied`，headless 里看门狗 15s 后说人话（截图 `scratch/evidence/record-nocam-t16.png`）。<br>**2026-09-13 加了第二条路：视频文件**（`?video=<url>` 或选文件，另有 `&name=` `&save=1` `&delegate=cpu` `&model=`）。逐帧 `seek`、不跟实时播放，所以序列完整；其余（PoseLandmarker、`world: []` 的空帧语义、写回 `/__demo`）与摄像头路共用同一段代码。**跑通过两段**：`pose-jumpingjacks`（1357 帧）与 `pose-walkturn`（609 帧）都由它产出并写回，骨架叠加线贴在人身上的截图见 `scratch/evidence/`。摄像头那条路仍然欠一个真人 |
| kiosk 外壳无人降帧（`src/shell/idle.ts`） | `stable` | 无人 300s → 渲染降到 10fps，人一回来立刻满帧；采集端（webcam/replay 都改了）上报在场，鼠标键盘也算有人。手动步进 rAF 的单测证明"1 秒只跑 11 帧"而不是"打算降"（`packages/app/test/frame-loop.test.ts`、`idle.test.ts`，随 `npm run check` 跑）；`/dev/degrade.html` 上手动触发实测 fps 130 → 9.5（目标 10），记录在 `scratch/evidence/degrade-ladder-t16.log`。**阈值 300s/10fps 是 `shell/idle.ts` 的局部常量，没进 `tuning.ts`（要收口的人点头）** |
| **连续出错降级阶梯（`src/shell/degrade.ts` + `safe-frame.ts`）** | `stable` | 以前只打一行日志，现在真降：连续 N 帧出错 → 关后期（`readFlags().nopost` 翻真）→ 再错 N 帧 → 占位几何 → 再错 N 帧 → 重载（会话内最多 2 次，防重载循环）。三级在 `/dev/degrade.html` 注入"每帧抛异常"的假 tick 后逐级触发，事件日志与状态面板读数抄在 `scratch/evidence/degrade-ladder-t16.log`（`data-sb-degrade=reload`、nopost/placeholder 双真）；另有单测 `packages/app/test/degrade.test.ts`、`frame-loop.test.ts`。各级动作用 `registerDegradeHandler()` 认领，**舞台/creature 还没认领（要在 main.ts 收口时接）**，在那之前第 1/2 级只翻状态位 + 发 `sb:degrade` + 写 `<html data-sb-degrade>`。**2026-09-13 降级审计复核（docs/36 §D4）：上面那句"还没认领"至今仍然成立，而它的后果此前没人复述过 —— `packages/app/src` 里 `registerDegradeHandler` 的调用数是 0，第 1 级翻的 `nopost` 只经 `readFlags()` 消费而 `stage.ts:169` 开机读一次就不再读，`state.placeholder` 零消费者。所以正式程序里的阶梯实际是「空转 → 空转 → 重载」，只有第 3 级是真的。`/dev/degrade.html` 的那份日志仍然有效，但它证明的是"事件发得出去"——那个页面自己注册了三个处理器——不是"有人接"**。**2026-09-13 接上（docs/36 §5③ 已闭）**：`src/shell/degrade-wire.ts` 把第 1 级接到 `stage.setPost(false)`（控件条「渲染」组按 `P` 的同一条路，**不是**翻 `flags.nopost` —— 那个位 `createStage()` 开机读一次就不再读），第 2 级接到 `creature.remorph(toPlaceholderGenome(genome))`；`main.ts` 在 creature 建好之后调一次 `wireDegrade()`，并且 `morph()` 在已降级时不再把真几何装回来（降级是单向的）。**在正式程序上实拍过一次真的连续出错**（`vite preview` 的 dist，`/?demo=1&debug=1&theme=porcelain&tier=2&seed=777&clip=jumpingjacks`，用 CDP 把 `GPUCanvasContext.prototype.getCurrentTexture` 换成每帧抛异常，等两级降完立刻还原让帧循环恢复，再读 HUD）：**接上之前 171k 面 / 36 draw → 降完仍是 171k 面 / 36 draw**（阶梯说了"回落占位几何"而画面一点没变，这就是"空转"的读数）；**接上之后同一次注入 171k 面 / 36 draw → 47k 面 / 28 draw**，HUD 多出 `degraded 回落占位几何` 与 `errors 60`。控制台三级齐全（`连续 30/60/90 帧出错`），没有任何一条 `处理器自己炸了`。单测 `packages/app/test/degrade-wire.test.ts`（含那条 grep：`src` 里必须存在非 `degrade.ts` 的注册者）|
| **`?selftest=1` 开场自检页（`src/shell/selftest.ts`）** | `experimental` | 逐条检查 WebGPU / 摄像头权限 / parts.json / demo 片段 / 本地模型，✓⚠✗ 各带一句人话；视觉按 `docs/23 §0`（等宽、两级字号、48px 安全边距、无圆角无图标）。不进主程序，主程序起不来也能开（独立 8.3KB chunk）。每条检查 8s 超时 —— **串行跑，一条挂住会把后面全部钉死在"检查中…"**（headless Chrome 的 `enumerateDevices()` 真的会挂）。实测截图 `scratch/evidence/selftest-t16.png`：parts.json ✓ 191 件/23 主题 · demo ⚠ 只有合成数据 · 本地模型 ⚠ 缺 · 摄像头 ⚠ 超时（headless）/ ✗ 权限被拒（浏览器面板） |
| `npm run kiosk` 一条命令进现场 | `stable` | = build + **本机 production preview + 慢回路宿主** + 自动开 `/?kiosk=1&preview=on`。Server 绑 `127.0.0.1`，`/__slow` 另按 socket remoteAddress 只认 loopback；花 credits / 写盘的口子不向局域网暴露。`npm run kiosk:fake` 走同一份 dist 与 preview hook，但以现有同槽位件代替 Rodin，不烧 credits。`/demo/index.json` 仍由 vite 插件在 build 时生成；另有 `npm run selftest` 直接开自检页 |
| **慢回路服务端 `/__slow`（提交/轮询/glb/血统池）** | `experimental` | dev server 中间件（`vite.config.ts` 登记，逻辑在 `packages/factory/src/slow.ts` + `slow-http.ts`），协议与数据形状见 `docs/17-SLOW-LOOP.md`。`SLOW_FAKE=1 npm run dev:slow` 下 curl 端到端跑通：POST → submitted → 4s 后 ready → `/__slow/part/<id>.glb` 200 `model/gltf-binary` 24,928 B，件的 aabb `y∈[0,1]`、X/Z 关于 0 居中（= 规范化契约本身），`localGirth=0.8524` / 2,890 tris；血统池 `lineage.json` 记下了 session / 时间 / 物种 / 槽位。拒绝路径六条各打一遍全是结构化 JSON（429 BUDGET_SESSION · 400 非 PNG · 400 槽位不在 targetSlots · 404 路径穿越 · 404 无此任务 · 405 GET 提交）——取证 `scratch/evidence/slow-loop-dev.log`。8 条测试随 `npm run check` 跑（`packages/app/test/slow.test.ts`）：端到端、HTTP 外壳、看门狗超时、Rodin 报错+退款、两条预算闸门、**未焊接的 6000 面网格走减面兜底仍然 ready**。**没打过一次真的 Rodin**（这个 worktree 里没有 `.env`，见 Risks）；`/about` 上那个「规格」标记因此只能摘一半 |
| **生产产物不携带慢回路，本机 preview 可显式挂宿主** | `stable` | `npm run build` 后 `grep -rl "__slow\|slow-http\|RODIN_API_KEY" dist/` 是空的；普通 `vite preview` 的 `/__slow/lineage` 实测 404 + `{code:'DISABLED'}`。同一 dist 上 `SLOW_ENABLE=1 SLOW_FAKE=1` 时 lineage 200，`GET /__slow` 是 handler 本身的 405 `METHOD`，不是 SPA 回退。Vercel 不跑 preview hook，仍然真 404 |
| 慢回路前端（提交/轮询/热插拔/血统抽取） | `experimental` | 两端早已闭合；2026-09-17 补上长开机的观众所有权：每位观众换 epoch + 匿名 session，离场 abort 本地轮询，旧任务可留进血统池但绝不 graft 给下一人；过期 / 身体消失的 geometry 当场 dispose。mass / swarm 在 PNG/POST 之前短路，不再白烧 credit 或播假的到货反馈。没有 mask（回放 / 分割器降级）时从逐渲染帧空轮询改为 1Hz，mask 晚到最多等一秒；600 帧 / 10 秒的确定性测试最多各读 11 次 body / mask、零请求，随后只提交一次。2026-09-18 把轮询改成显式单张 demand：武装才通知 Capture，取走后归慢回路所有并在 PNG 编码 finally 中 close，离场 / 永久关闭撤需求；旧 generation 的迟到图由 Capture 关闭。非法 `dt` 不会提前武装或击穿重试门，采集边界同步抛错只关闭慢回路，重试倒计时不跨观众。`slow-client.test.ts` 10 条 + 服务端 8 条 + 宿主 3 条 |
| Stage / 后期 / 场景 | 见上面那一组 | T-09 已落地，舞台美学这一轮把地平线/接触阴影/粒子/多场景做完；仍欠：接进 `main.ts`（`stage.render()` / `stage.frame()` / `stage.timeScale` / `stage.setScene()`）、真人实测、现场投影亮度、脚部件与骨架落点的 14cm 缝 |
| 开场选择页（SDF 环） | `experimental` | `/dev/choose.html`：6 张卡滚/选/进，`?theme=xeno` 跳过，数字键直选，空闲自动选（`?idle=6000` 验过）；截图 `scratch/evidence/choose-0{1-settled,2-entry,3-hover-trail}.png`。实现在 `packages/app/src/choose/ring/`（`sdf.ts` / `field.ts` / `atlas.ts` / `first-screen.*`），移植自 **Viscose-carousel**（MIT，署名保留；`public/` 素材一张没拿）。**没有 `src/vendor/` 目录，也不该有**：上游是 GLSL + React，这边是 TSL + 原生 TS，没有一行逐字搬过来，所以出处写在三个文件头和 `docs/35-VISCOSE.md`，而不是一个会谎称"这些文件是上游原样"的 vendor 目录。上一版 `dither-blur-carousel` 的 `src/vendor/dither-carousel/` 已随那次重做删掉。未验：真实现场投影分辨率与触摸屏。**2026-09-13（docs/39 §2.1 已闭）**：上场判据从 `choose.ts` 里拆到 `choose/wearable.ts`（`choose.ts` 第一行 `import '../ui/type.css'` 让它在 node 里 import 不进来，于是这条判据此前没有任何办法被单测钉住），并且拿掉了「∪ 有 anchor 图的」那一条 —— anchor 图是一张参考渲染，不是一件可以穿的零件。今天的真索引上这一改**一个条目都没多没少**（26 张卡，正式程序上实测 `.sb-row` = 26），它防的是下一次：谁给一个零自有件的条目补一张 anchor 图，它立刻进轮播，而 `makeGenome` 那边一件自有件都没有。`packages/app/test/choose-wearable.test.ts` 拿**真的** parts.json 钉住那条一致性命题：上场的每一个条目，`makeGenome` 在 tier 1/2/3 下都装配成它自己 |
| **选择页举手滚动（`?wave=on\|off`）** | `experimental` | 现场一件输入设备都没有 —— 在这之前，站在装置前面的人**翻不了名单**，只能等 30 秒被随机塞一具身体。现在：举**一只**手过同侧肩 0.10m、停住 **0.75 秒**、横向挥 = 翻名单。走的是**滚轮那条路**（新增 `field.nudge()`，滚轮自己也改成调它），所以惯性 / 阻尼 / 槽位吸附全部复用，不存在第二套手感。三道防误触发：过肩线（挡边说话边比划）、连续 0.75s（挡走过去的路人）、**只能一只手**（挡伸懒腰 / 举双手拍照）；松开侧有 12cm 滞回 + 0.35s 宽限。「它武装了」不画任何新东西（docs/26 §F 反面清单）：借用这一页本来就有的**光标软化** —— 蓄势那 0.75 秒里那团软化从无到有，位置跟着手沿卡片行进的那根轴走。**真实鼠标永远优先**。判断是纯函数（`src/choose/ring/wave.ts`），接线在 `wave-input.ts`；坐标换算只走 `mediapipeToWorld()`（P4）。证据：`packages/app/test/wave.test.ts` 13 个用例随 `npm run check` 跑 —— app **127 → 142 pass / 0 fail**，core 146 pass，`check:parts` 208 件 0 错，整条绿。红-绿都验过：把蓄势与双手判定拿掉（立即武装）→ **13 中 5 红**；把死区与跳变门限拿掉 → **13 中 2 红**；把 `?wave=` 改成原样透传（不校验）→ flags **8 中 1 红**；三次还原后全绿。整条链在真实的环上跑过（`/dev/choose.html?wave=sim`，合成姿态）：`__ring.debug()` 读到正面卡 3→2→1→0 走过 4 个物种、`state.spin` 摆幅 0.548 rad，页面自己的 rAF 帧率读数 110–119fps（不开手势时同一页 117fps）；不开手势时 `spin` 增量 0.0000（名单绝不自己动）；`?gl=off` 下是 29 行 DOM 列表、键盘照常、无环无手势。**没有真人验过**（P17）：手抖、逆光、遮挡、以及「0.75 秒会不会让人觉得没反应」都还是纸面的；`WAVE` 那组阈值是本文件局部常量，**没进 `tuning.ts`**（要收口的人点头） |
| 选择页无 WebGL 降级（DOM 列表） | `experimental` | `/dev/choose.html?gl=off`：6 张卡列出、点选写 `?theme=`、键盘与自动选择照常；控制台 `mode=fallback`。排版已并入 `type.css`（等宽、同底色、卡片图 —— docs/23 §S2「降级路径也是作品的一部分」），列表模式下底部常驻名牌收起、提示语改写成"点一行即确认"。截图 `scratch/evidence/ui-choose-fallback.png`。真实的 context lost 分支 `Not run`。**2026-09-13 降级审计（docs/36 §D2）：`?gl=off` 只存在于 `/dev/choose.html`（`dev/choose.ts:98`）—— `readFlags()` 里没有 `gl` 字段，`main.ts:195` 那次 `chooseTheme()` 也不传 `forceFallback`。正式程序上实测 `/?gl=off` 完全无效（`.sb-ring` 照挂、`.sb-row` 为 0）。这一行的证据因此只覆盖 dev 页，不覆盖观众打开的那个 URL**。**2026-09-13 接上（docs/36 §5② 已闭）**：`Flags` 加了 `gl`（`?gl=on|off`，认不出来的值按没写过处理并 warn，规矩同 `?scene=`/`?cam=`），`main.ts` 那次 `chooseTheme({…})` 传 `forceFallback: !flags.gl`；`choose/**` 的行为一行未动（`ChooseOptions.forceFallback` 本来就在）。**在正式程序上 A/B 实测过**（dist + `vite preview`，headless Chrome，`navigator.gpu` 可用）：`/?demo=1&nav=0&loading=0` → `.sb-ring` **1** / `.sb-row` **0**；同一次构建 `/?gl=off&demo=1&nav=0&loading=0` → `.sb-ring` **0** / `.sb-row` **26**，控制台 `[choose] 环不可用，走 DOM 降级列表： forceFallback`。`choose.ts` 的文件头也跟着改了 —— 它此前拿这个参数当"这条路跑过了"的证据，而那是一句关于正式程序的话 |
| **选择页 30 秒自动选的倒计时（docs/23 §S2）** | `stable` | 规格要的是"最后 **5** 秒、中心卡下方一条**极细的进度线**"；原来是右上角一行"10s 后自动选择"的文字。现在是 1px 横线，60ms 步进（200ms 肉眼能看出台阶）。截图 `scratch/evidence/ui-choose-countdown.png`（`?idle=7000`，快门落在倒计时中段） |
| **可选条目 < 3 → 螺旋退化成横向一排（docs/23 §S2）** | `stable` | 以前没做：两个条目也照样进螺旋，看起来像一个转不动的轮子。现在走同一条 DOM 列表路径（键盘/自动选择/退出动画全照常），只是排成一排。用新开关 `/dev/choose.html?n=2` 当场跑得到 —— 没有这个开关这条降级路径永远不会被验证。截图 `scratch/evidence/ui-choose-row-under3.png` |
| **全部页面共用 `src/ui/type.css` + 极简页头（`src/ui/page.ts`）** | `stable` | `/dev/{index,parts,figure,mass,choose,stage,capture,degrade,anchor}.html` 与 `?selftest=1` 全部改成 `<link>` type.css，各自的字号/颜色常量删光（自检页原本把 §0 那一套抄了第二遍）。页头两种形态：文字页在文档流里，满屏 canvas 页压进左上角安全区并在 4 秒后淡下去（截图里不留调试文字）。截图 `scratch/evidence/ui-{index,selftest,figure,mass,stage,capture,degrade}.png` |
| 形态空间排布（按 `axes` 绕质心成环） | `experimental` | `?roster=1` 下 23 个条目排成一圈（autonomous→wheelleg→patrol→field→…→orb）；旧版 parts.json 无 `axes` 时退回数组顺序，也验过 |
| **声音四层（`src/sound/`，docs/29）** | `experimental` | 在场 / 运动 / 升档 / 慢回路等待，全部 Web Audio 合成，**这四层零素材文件**（`assets/sound/README.md` 里论证了为什么；离散音那两层用素材，见下面两行 —— 原文这里写的是"零素材文件"，在第五层落地之后就不再准确了）。离线取证 8 张：`scratch/evidence/sound-{presence,motion,tier,wait,act-follow,act-echo,act-resist,act-facing}.png`（波形 + 对数频谱，跑的是 `buildSoundGraph` 那一份图本身）。实测：运动层静止 −29.4 → 峰值 −20.3 dBFS（+9.1dB）；在场 −34.8 → −30.2；升档 −29.2 → −18.0；四个玩法在同一条运动曲线下 −19.0 ~ −21.8 各不相同。两段可听的 wav：`sound-motion.wav` / `sound-tier.wav`。**只在合成信号上跑过，没接过真人；也没在现场音响上放过**（docs/09 里因此多一条未知：房间声学） |
| **声音靶场 `/dev/sound.html`** | `stable` | 上半手推每一个信号 + 四层电平表，下半 `OfflineAudioContext` 取证。截图 `scratch/evidence/sound-range.png`（静止）与 `sound-range-live.png`（speed 1.4 / jerk 15 / resist / 等待中 → 身体 1.00、等待 1.00）。`?mute=1` 下劫持 `window.AudioContext` 计数，点遍所有按钮后仍是 **0** |
| **第六层 · 工作声（`src/sound/work.ts`，docs/29 §2.6）** | `experimental` | 慢回路 `phase === 'running'` 的那 30–90 秒里，隔壁那间屋子的稀疏接触声：三个 CC0 变体轮换、900Hz 低通、间隔 `3.1 ± 1.6s`、`leadIn` 2.2s、**不加速不规律**。**排程是纯的，11 条 node 测试**（密度落在 90 秒 20–40 记、前后半段平均间隔比 > 0.65、间隔变异系数 > 0.15、连着两记不同采样、同 seed 可复现、掉帧不补发）。变异检验：把 `leadIn` 与"停在等待结束"那两处代码删掉 → 2 红；把 `gapJitter` 归零并让变体随机重复 → 3 红。**没有接过真人、没在现场音响上放过、听感侧的离线图这一轮没有重跑**（见 docs/29 §9 那条警告）。接线：**没有改 `main.ts` 一个字符** —— 它挂在已有的 `sound.update({ waiting })` 上。旋钮这一轮从 `work.ts` 的 `WORK` 移交给了 `SOUND.work`（`WORK` 现在就是它本身） |
| **第五层新加的两记 `reveal` / `ground`（docs/29 §2.7 / §2.8）** | `experimental` | **触发点已接**（上一轮是 `stub`）。`reveal`：`main.ts` 里 `chooseTheme(...).then(handle => ...)` 中紧贴 `loading.finish()` —— 选择页**已经在屏幕上**的那一刻；`handle` 为 null（URL 里已有主题、这一页没出现过）时不响。`ground`：帧循环里紧贴 `stage.frame()`，判据 `sound/ground.ts` 吃的是**画接触阴影用的同一批落点**（`stage/framing.ts` 的 `contactPoints()`），**不是 `speed` / `energy`** —— 从 speed 推出来的是"动得快就响"，观众一听就知道对不上（P21）。三条规矩：只在多出一个落点的那一帧响（抬脚不响）、第一次观测只立基准（进场不砸一下，`presence.justReset` 时 `reset()`）、120ms 防抖。**判据是纯的，10 条 node 测试**（站着不动 300 帧 0 记、抬脚 0 记、落脚恰好 1 记、悬空 120 帧 0 记、0.15m 处不算着地、两步两记、reset 后不补、阈值夹在 0.03m 与 `STAGE.contactLiftRange` 之间）。红→绿：新断言先红（`ERR_MODULE_NOT_FOUND` / `TypeError`），实现后 app 185 → **195 全绿**。变异检验四发四中：去掉"只立基准"→ 3 红、去掉防抖 → 1 红、把"多出一个落点"改成"落点数变了"（抬脚也响）→ 3 红、不夹 `threshold` → 1 红。**仍然没有接过真人、没在现场音响上放过；`scratch/evidence/sound-cues.png` / `.wav` 仍是旧的**（docs/29 §9 那条警告说明了为什么，并补了十一格的实测峰值） |
| **离散音素材共 9 个文件 18.9KB（19,326B）** | `stable` | 全部 CC0 1.0，逐条来源 URL / 作者 / 处理步骤在 `assets/sound/README.md` 的七列表里。这一轮新增 5 个共 **10,982B**（`reveal` 2,284 / `ground` 2,407 / `work-a` 1,830 / `work-b` 1,921 / `work-c` 2,540，`ls -l` 量的）。授权存证：前四个是 `licenses/*.png`（截图），**这一轮五个是 `licenses/*.txt`**（同一 URL 的页面原文 + 抓取时间 + 抓取命令 —— 这条 lane 上没有浏览器；原文可 grep 可 diff）。一条 CC-BY-NC 的候选（365863，声音最合适的那一条）**因为授权被否**。`test/sound.test.ts` 有一条断言把整层钉在 < 64KB |
| **自动播放策略 / 现场关声** | `stable` | 主程序 `/?demo=1&debug=1&theme=patrol&seed=99` 开机日志 `sound=locked`，控制台**没有任何被拦截的音频警告**；点一下后转 `running`；按 `m` 打出「静音」「恢复」各一行。帧开销：一次控制 tick 中位 **4µs / p95 8µs**（真实 AudioContext，20×50 次取分位），`controlHz=30` 下两帧才发生一次 |
| Web 部署（Vercel + serverless 慢回路） | `spec-only` | T-19，检查单在 `docs/13` §6 |

## 慢回路

| 表面 | 状态 | 证据 |
|---|---|---|
| **`/__slow` 服务端（提交 / 轮询 / 规范化 / 血统池 / 预算闸门）** | `experimental` | `SLOW_FAKE=1` 下 dev server curl 端到端：POST → submitted → ready → `/__slow/part/<id>.glb` 200 `model/gltf-binary`；aabb y∈[0,1]、X/Z 居中（= 规范化契约）。六条拒绝路径全是结构化 JSON。8 条测试。生产构建下 grep dist 无 `__slow`；普通 preview 为结构化 404，只有本机 `SLOW_ENABLE=1` 才挂同一 handler |
| **慢回路前端（`src/slow/slow.ts`）** | `experimental` | 靶场 `/dev/slow.html` 实跑：剪影 → 提交 → 轮询 → 取 glb → `graft()`，6.3 秒闭合；**第二次跑抽到了第一次留下的那件**，跨会话血统持久一并验掉。截图 `scratch/evidence/slow-frontend-e2e.png`。只有 mask 是画出来的，其余全是真路径 |
| **血统（前人的件进下一个人的候选池）** | `experimental` | `lineage.json` 记 session（匿名）/ 时间 / 物种 / 槽位；`GET /__slow/lineage` 连跑两次分别返回 1 件、2 件 |
| **一次真实的 Rodin 调用** | `spec-only` | **从没打过。** 离线端到端验的是**回路**，不是**生成**。"把实时 AI 3D 生成放进交互回路"这句主张里，被验证的是"回路"那半。docs/09 U10（端到端真实耗时）因此仍空着 |
| 现场 production kiosk 的慢回路宿主 | `experimental` | 已接通；关/开两态与 fake handler 均在同一 dist 上实测。真 Rodin 仍 Not run（需 key + 约 0.5 credit），所以还不升 stable |

## 存档（`docs/43 §8` 的 A 档）

| 表面 | 状态 | 证据 |
|---|---|---|
| **一行到访记录的形状（序号 / 物种 / 粗到天的日期）** | `stable` | `packages/archive/src/visit.ts`。`docs/43 §9.4` 那条「永久保留」是靠**这一行里根本没有个人数据**站住的，所以它有仪表不只有注释：`app/test/archive.test.ts` 18 条，钉字段清单、`species` 的闭集、日期只到天、请求体塞 `ip`/`ua`/`email`/`lat` 一个都不落库，外加扫源码确认 `packages/archive/**` 与 `api/**` 里没有任何一处读 header / socket / cookie |
| **`POST /api/visit` · `GET /api/visits?limit=`** | `experimental` | 返回形状逐字照抄 `GET /__slow/lineage`（`{ok,total,entries}`）。逻辑一份（`packages/archive/src/http.ts`），三个宿主共用：Vercel 函数（`api/*.ts`）、dev server、`vite preview`。**Vercel 上那一侧没有实跑过** —— 这条线不许开通任何东西，见下一行 |
| **线上存档 Worker（`packages/archive-worker/`，Cloudflare Worker + D1）** | `experimental` | `docs/43 §9.3` 2026-09-14 重裁。同一份契约（`route.ts`）和同一份 `visit.ts`，`app/test/archive-worker.test.ts` 17 条：入口模块只导出 `default`（workerd 否则拒绝启动，`wrangler dev` 当场报过）；扫源码不许拷贝闭集校验；请求头塞满 IP/UA/cookie，`node:sqlite` 上的真表里每一张表零字节；`visits` 表恰好三列；40 个并发 POST 拿到 1..40（旁边一个先读后写的对照必须撞号，证明替身会交错）；只读 `Origin` 一个 header、不碰 `request.cf`、不打日志；写只认正式地址；限流键是常量、数和 `tuning.ts` 对齐；网站找存档的顺序（同源 → Worker → 缺席）。**没有部署过、没有连过真 D1** —— 部署是作品负责人的动作，步骤在 `docs/45` 乙；`ARCHIVE_WORKER_URL` 在那之前是 `null` |
| **存储** | `stub` | 接口两个方法（`store.ts`）。`fileStore` 只追加 JSONL，dev / preview / 装置那台机器走它，有测试（换一个实例读回来、一条 < 80 字节）；`restStore` 按 Marketplace 上那一家的 HTTP 口子写好了，**协议有测试**（只发 `INCR`/`LPUSH`/`LRANGE`/`LLEN`，没有任何一条会让这一叠变薄），但**没有连过真服务**。开通是作品负责人的动作，步骤在 `docs/13 §5.1` |
| **没开通时线上是什么样** | `stable` | `createVisitStore()` → `null` → 两个端点 404 `{code:'DISABLED'}` → `/lineage` 仍是「这条回路只在装置现场活着」。**故意不退回内存计数器**：那个数会随函数实例重置，而这一页头一行写着「这一叠不会变薄」（P21） |
| **写入时机与降级** | `experimental` | 弧线走完（`ArcState.held`）写一次，在 `requestIdleCallback` 里；`justReset` 收回来，下一位重新算一场。任何失败（404 / 5xx / 断网 / 超时）= 静默关掉、本次会话不再尝试、不重试。没有 `sendBeacon`（`§9.7`），有测试扫着。**摄像头没打开的那一场一行都不写** —— 那就是 `§9.5` 的「不参与」 |

## 展陈层（作品自己讲自己的那几页）

| 表面 | 状态 | 证据 |
|---|---|---|
| **换页与导航**（`ui/transitions.ts` 纯表 + `ui/page-transition.ts` + `index.html` 首帧底色；docs/47） | `experimental` | 2026-09-14，Chrome 152 无头，`scratch/transitions/measure.mjs` 走 27 跳，前（`8842a91`）一轮、后两轮，服务端 `scratch/transitions/serve.mjs` 照 `vercel.json` 发。首帧底色与同文档交棒两轮 27 跳 0 白帧。**跨页过渡开着**（第二轮，§4.3）：有头 Chrome 修根因后 7 轮 + 加门槛后 2 轮，最终 30 跳 0 白；修根因后的无头十轮未跑。导航：每张 html 走得到一个出口构件，侧室挂目录（**原先那条「侧室不挂目录」的证据已不是现状**），回到大厅落在选择页，404 页。**没量**：真 GPU 窗口、预渲染收益（CDP 连着时 Chrome 关预渲染）、线上压缩字节。胶片 `scratch/transitions/filmstrips/` |
| **《共生护照》`/passport.html`** | `experimental` | 做成一本**签证页**而不是记录表：四枚章，I 拒入（人类否掉 100 件批次的前提，逐字引用）、II 准入（Rodin 两个没焊的网格）。II 保留的理由写明是**能力不是产物** —— 容差焊接这条路是它逼出来的。**IV 拒入（2026-09-14 加）是唯一一枚申请人不是产物的**：花名册上那个故意的空位 `guest.founder`（`docs/14 §2`）。它此前只是**缺席** —— 门挡住了它，但观众在任何一页上都遇不到那个缺口；`docs/39` 要的是「看得见地缺席」。裁定人写的是 `clearance` 门本身，因为四枚里只有这一枚**还在执行**（每次 `factory:index` 都重盖一遍）。外形上和另外三枚只差一处：末尾一条没有人签的空线（`.pp-blank`）——不灰掉、不加锁、不引入第二种语义色，灰掉读作「你没有权限」，而这里的意思是「我们没有去拿」。守卫 `packages/app/test/vacancy.test.ts`（4 条，逐条先红后绿）：门还在 `buildIndex()` 的源码里、产物里条目与零件都没有它、`makeGenome` 随机 400 seed × 3 tier 抽不到它、`?theme=guest.founder` 会 warn 且交回一个索引里真有的条目。取证：dev server 全页截图（1440×5600）+ 420px 窄幅。顺带改掉一处窄了的目录文案：`COPY.nav.items.passport.answers` 的「哪一件**产物**被拒绝入境」→「谁被拒绝入境，谁被放行」（第四枚的申请人不是产物；新写法正好是 `nav` 那段注释自己举的例子）。进构建产物（`dist/passport.html`）。页面已在浏览器里实渲并全页截图 `scratch/evidence/passport-full.png`（1280×2600）；页上 5 处存证逐条核过：`19880ae` / `4860281` 两个 hash 解得开、`docs/09 U11` 在、`weldTolerant()` 在。核出并修掉一处引用错误（未焊接网格指到了 `docs/07 §3`，那节是错误码与重试，正确的是 §3 第 3 条）|
| **共创过程档案 `/making.html`** | `stable` | 28 个 commit hash 逐个 `git log -1` 核过全部解得开、印在页上的时间与 `%ad` 一致。六件互相纠正、编排者自己的四个错、有代价的三次判断，全部指到提交。专列一节写**想写但没挖到证据所以没写的四件事**（九条线的名单、每条线的时长与 token、被驳回的提议、作品成立与否）。截图 `scratch/evidence/making-0{0..5}.png`（整页 1440×12136） |
| **五个展陈面的事实复核（2026-09-13）** | `stable` | 逐条重新推导，不抄上一版的数。`npm run check:parts` → **220 件 0 错**；`node packages/app/poster/build-data.mjs` → **28 条目 · 220 件（剔 12）· 9 种身体方案**（合入 main 之后重点了一遍：那 11 条提交里有一批新生成的件与一条新的剔除）；两个包的测试 **core 182 + app 250 = 432 绿**。改掉的过期断言：展签与 `/about` 的「约 90 秒」→ **约三分钟**（`tuning.ts` 的 `ARC.total = 180`）；`/about` 的「四种形体」→ **九种**；`/making` 的数字栏 191 件 / 23 条目 / 52 提交 / 129 测试 / 25 文档 / 94 TS → **220 / 28 / 249 / 432 / 45 / 181**（并给那一栏盖上推导日期），「踩出来的十条」→ **十一条**（P21 已立）。**没有**动的：时间线与 P13/P15 里的「186 件」、护照第 I 枚章的「23 个物种 · 100 件」—— 那些印在「当时的样子」一栏里，是证据不是现状（本文件表头第 2 段）。新内容：`/about` 多一节「三分钟」（四个乐章 + 那条永不脱钩的规矩 + 物质那一条），`/making` 多 11 条时间线、2 件互相纠正（驳回方也是代理）、P21，`/passport` 多第 III 枚章（裁定人是自查那条线）。**修掉一个真 bug**：`/passport` 的双语块是手搭的两个 span，类名对但漏了 `sb-cjk` —— 整页汉字一直没吃到那 0.06em 的补偿（和选择页名牌那次同类）；现在走 `setBi`。取证 `scratch/evidence/page-{entry,about,making,passport,lineage}.png`（本机 dev server，端口自选）|
| **侧室 `/parts` `/roster` `/marks`（docs/23 §S9.1，2026-09-14）** | `experimental` | 三个仪器被展出，判据是「它回答的那个问题观众也有」。**不进目录**：七个面还是七个。URL 搬到根目录（`packages/app/{parts,roster,marks}.html`，vite 的 `pages()` 本来就扫根目录，零配置改动），`/dev/parts.html` 因此不再存在 —— `npm run build` 后 `dist/` 里是 `parts/roster/marks.html` + `dist/dev/` **15** 个（原 16 个减掉搬走的 parts）。门是 `/about` 正文里的数字（`ui/clue.ts`）：**实测 31 扇** —— 计数条 3 扇（28→`/roster`、9→`/marks`、230→`/parts`；「槽位 10」「材质 9」故意没有门），编号对照表 28 扇（每个号 → `/parts#t-<id>`）。逐条实测（本机 dev server :5231，Chrome）：静止态 `box-shadow: none` / `background-size: 0px 1px` / 无下划线 / 无边框，且字色字号与旁边**没有门**的那个数逐字相同；鼠标悬停到「28」上底线长到 `100% 1px`，旁边四个数一动不动；键盘一次 Tab 就落到第一扇门上，`:focus-visible` 为真、底线同样长到 `100%`、**浏览器焦点环留着**（`outline: auto 1px`）；点「28」落到 `/roster`（28 格）；`/parts#t-char_painting` 定位到「画 Painting」那一栏，28 个锚点齐全。读屏取证：辅助功能树里读得到「侧室 · 物种接触表」等 31 条目的地（`find` 查 `侧室` 返回 20 条上限）。三个房间都戴 `heroMeta('/about')` 的横带、都**不挂**目录（实测 `.sb-nav` 为 0），`test/page-header.test.ts` 多两条断言钉住。`/marks` 的九枚记号来自 `src/ui/marks.ts` —— 那是另一条线（`worktree-agent-a0d21ec94b6bf12ad`）作品负责人已逐枚看过的那一份，**逐字取来的同一个文件，不是重画的一份**；它还没进 `main`，所以在那条线落地前 `/about` 的散点与图例仍是老的四分支 switch（六个方案共用一个空心圆），不一致的那一侧是 `/about`。截图 `scratch/evidence/room-{parts,roster,marks}.png`（1440 宽全页，dev server 无 WebGL，缩略图按设计退成比例剪影） |
| **构建入口自动发现（根 `*.html` + `dev/*.html`）** | `stable` | 展陈层的页面分几条线并行加，写死 input 表既是冲突点也会漏页。现在 `pages()` 扫两处目录，放一个 html 进来就是一页，没有第二处登记。`npm run build` 后 `dist/` 有 `index/passport/making` + `dist/dev/` 10 个 |
| **加载态 `src/shell/loading.{ts,css}`（docs/23 §S0）** | `experimental` | 在这之前从"打开 URL"到"身体出现"之间观众看到的是**一块黑屏**（`/dev/figure.html` 是一行裸 `loading…`）。现在是三档真实进度：`正在点亮画面`（渲染器造出来 0.4 → `init()` 完成 1）/ `正在准备零件`（`parts.json` 0.15 + 选择页 anchor 图到货数）/ `正在认识你的身体`（采集端自报的启动里程碑，webcam 3 件、replay 2 件）。**600ms 宽限期内一帧都不画**，快的时候观众仍然看不见它。慢网取证（Chrome DevTools Protocol 真限速、禁缓存，`vite preview` 打的 dist）：`scratch/evidence/ui-loading-1-early.png`（14%，40% / 15% / 等一下）、`-2-mid.png`（25%，好了 / 18% / ···）、`-3-late.png`（55%）、`-4-slow-first.png`（8 秒后「再等一下，网有点慢」）、`-5-slow-second.png`（20 秒后换第二句）、`-6-fastnet.png`（不限速）、`-7-handoff-to-choose.png`（选择页一上来它就让位，螺旋构图未动）。**两条硬规矩写在文件头**：进度必须来自真实信号；没有真信号的地方宁可停住（显示 `···`）也不许用定时器往上爬 |
| **加载 → 选择页交棒（2026-09-19）** | `experimental` | `finish()` 改为节点真正移除后才 resolve 的幂等 Promise；anchor 依旧并行预取，但 chooser DOM、输入、空闲计时和环的 reveal 全部等 `beforeReveal` 闸门。修正前 `sb-first-screen` 会把 loading 底变透明，所以 100% 与后面已开播的选择 UI 重叠不是偶发毛刺，而是确定竞态。假时钟测试逐段覆盖 1240ms（有展签）/ 1860ms（无展签、有字标）与异常 fail-open。真浏览器在点击「开始」后逐 rAF 采 31 帧：最后一个 loading 帧仍是 100% 且无 chooser DOM，紧接下一帧 loading 已移除、chooser 首次出现；DOM 重叠 **0 帧**，可见重叠 **0 帧** |
| **`library.load()` 与 `renderer.init()` 并行** | `stable` | 原来资产排在 `renderer.init()` 后面，而它俩毫无依赖关系 —— `init()` 是首次 pipeline 编译，那几秒里管子完全是空的。同一条 350 kbps 限速下端到端 **21.6s → 13.0s**（两次 `[loading] 加载完成，耗时` 读数）|
| **目录 `src/ui/nav.{ts,css}`（docs/23 §S9）** | `stable` | 在这之前首页上没有任何入口通向 `/about` 和 `/making`，而且那几页之间一条链接都没有。右上角一个词 + 一条线，展开是「名字 + 它能回答什么问题」的表，当前页标「在这里」且不是链接。挂在 `/`、`/about`、`/making.html`、`/passport.html` 四处。`?kiosk=1` 下 `readFlags().nav` 为 false，**根本不挂**；满屏画布页 4 秒后淡到 0.18。截图 `ui-nav-1-entry-closed.png` / `-2-entry-open.png`（压在入口展签之上）/ `-3-about-open.png` / `-4-making.png` / `-5-kiosk-absent.png`（现场模式下确实一个字都没有）。踩过一个实测出来的坑：靠 `text-align:right` 对齐时，展开会把「目录」两个字从右上角甩到表的左上角（实测 x 1484→1072），改成 flex + `align-items:flex-end`。**2026-09-15**：入口页的 `startOpen` 原来只看「是不是展签页」，不看视口宽度——`entry.css` 只在 621px 起才为目录让出右边一栏，620px 以下（手机、不少旧笔记本）目录仍铺开，整片压住展签。改成挂载时叠一次 `matchMedia('(min-width: 621px)')`，四档（375/620/621/800px）无头 Chrome 实测：620 及以下收起，621 起照旧展开；手动点开/点外面收起不受影响 |
| **右下角那一列 `src/ui/exits.{ts,css}` + `ui/exits-url.ts` + `acts/untether.ts`** | `stable` | 选完物种之后观众此前**没有任何出口**：换物种只能改地址栏，而现场没有地址栏；那个角上只有两行 4 秒就淡掉的 13px 灰提示。现在是三行 20px（`--sb-size-h2`）常驻的字：**回到大厅**（重载，`hallSearch()` 把 theme/plan 删掉、其余全部状态原样带走 —— 结论照抄 `ui/controls.ts` 文件头第 2 条，方向反过来用）、**把身体还回去**（`director.force('untether')`，身体改演自己的摇曳而**摄像头与采集一帧不停**）、**摄像头**（开 / 关，并且把「现在是不是在看着你」显示出来；左上角那个预览窗是另一条泳道的，这里只有控制，不做第二个预览）。颜色一律走 `--sb-ink-strong`，**没有一处写死的白**。`?exits=0` 关掉，`?kiosk=1` 下默认就不挂（无人值守的装置不该向公众提供「回到大厅」），认不出来的值当没写过并打一条 warn。证据 `scratch/evidence/ui-exits-readings.md`：同一份 CSS 在 `scene=void` 下解析成 `rgb(255,255,255)`、在 `scene=gallery` 下解析成 `rgb(0,0,0)`；`?debug=1` 的 HUD 在 follow → untether → follow 三态里 `infer` 是 31 → 30 → 31 Hz（采集没停）；回大厅之后 URL 是 `?demo=1&scene=gallery&debug=1&act=follow&vitality=1&refine=1`。`npm run check` 全绿（core 149 / app 135 / parts 208 件 0 错 2 警告）。**这一轮抓到的真 bug**：还着身体回大厅会把 `act=untether` 写进 URL，下一个观众的身体根本不跟他 —— 已在 `hallSearch()` 挡掉并立了断言。**没有截图**：这台机器上的 headless Chrome 连 `/about.html` 都截不出来，四条路各试一次的记录在那份 md 的第 4 节 |
| **控件条的叠加与控件表 `src/shell/intent.ts` + `src/ui/control-table.ts` + `src/ui/stage-url.ts`**（`ui/controls.{ts,css}` 变成视图 + 接线） | `experimental` | 2026-09-14，作品负责人裁定「没有一个按钮能锁住系统」（docs/23 §S4.1）。**玩法 / 形体是叠加**：按钮只增删 `intent`，导演每帧读它换线的采样点、身体到场读 `bodyAt()`；`director.force` 只剩「还回去」。`?act=` / `?plan=` 开机即叠加，人一走回到 URL 那一份。**控件表**是每个控件的唯一来源：面板分组、快捷键、键位条、重载写 URL（`stagePatch`）、回大厅（`hallSearch` 改用它）、随机池（`rollSlots`，玩法那一格只消耗、写成删除，旧 seed 的物种/形体/画面/描边不变）、回舞台白名单（`STAGE_KEYS`）全从它推；`readFlags` 不改，逐项往返核对。**链出工作台**：lineup / mass / vitality / figure，`?from=/?theme=…` 回到同一屏（`return-to.ts` 的舞台分支只认表里的键 + 普通字符，输出按白名单重拼）。**面板全透明**（作品负责人裁定）：没有一处底色，字 / 格线 / 选中线跟右上角的墨；状态靠墨强弱 + 线形（实线 / 点线 / 粗细）+ 等宽状态词（此刻 / 叠加 / 重开中），`aria-pressed` / `aria-current` / `aria-busy`。**面板让开右下角那一列**（`--sb-exits-h`）。单测 `intent` / `control-table` / `stage-url` / `controls-panel` / `controls-surface` 新增，`random-url` / `exits` 改写；先红后绿记录在交付报告 |
| **自己会消失的一行 `src/shell/notice.{ts,css}`** | `experimental` | 把 docs/23 里写了很久没做的两条落地：§S4 左下角物种名（截图 `ui-notice-species.png`：「异形 / Xeno」）、§S0 右下角「降级渲染」（**只有 DOM 取证**：`.sb-notice--bottom-right` 在、`opacity: 1`、rect `[1437,804,114,47]`；无头浏览器在这条路径上合成不稳，没拍到可信的图）。降级那句**故意等加载态收掉之后才说** —— 说早了会被那一层盖住。另外修掉一个会静默吃掉元素的坑：带 `fill-mode: both` 的入场动效在文档时间线不推进的页面上停在 `currentTime: 0`，元素永远透明；规矩写进 docs/23 §0 |
| **左下角那块实时读数 `src/ui/readout.{ts,css}` + 判据 `src/ui/readout-state.ts`** | `experimental` | 这件作品的整条链是一具身体被一台机器**读**成数，而观众只看得见结果 —— 那次翻译因此读起来像魔法，而"把一个人翻译成数据要付出什么代价"正是它的题目。左下角现在有一块半透明的灰面板，顶上一行**有人 / 无人**，底下五个数：**置信**（`RawPose.score`）/ **关节**（逐点 visibility 过 `REFINE.occlusionVisibility` 的个数 / 总数）/ **推理**（`Capture.fps`，不是帧率 —— 是"每秒重新看你几次"）/ **动能** / **舒展**（`MotionFeatures`）。**五个都是测量，一个作品的决定都没有**：fps / 三角数 / draw calls 归 `?debug=1` 的 HUD（那是机器的健康，不是这个人的身体，而且预算标红是我们的事）；乐章 / 弧线进度 / 档位 / 忒修斯 12/18 一个都不上来（docs/40 §5「不要给观众进度条」、docs/23 §S5「没有等级数字」、docs/44 §7「给操作员，不给观众」，以及 §7 那条更硬的："它拿走你刚用过的那一部分"是**唯一一处观众能主动验证**的机制，写在面板上等于替他做完那次发现）；speed / jerk 和动能是同一个轴的三个时间常数；symmetry / verticality / stillness **全仓没有消费者**；观测身高想要但不敢要 —— `observedHeight()` 在脚出画时退回 `SKELETON.referenceHeight`，从外面分不出测量和缺省值。**没有人的时候除推理频率外全写 `—`**（`lastFeatures` 会留着上一个观众的数，照常显示就是"空场里有人在动"）；模型不报逐点置信度时关节也写 `—`，不写 `0/33`（`overallScore()` 那时把 score 记成 1，会出现"置信 1.00 / 关节 0/33"自相矛盾）。**圆角 + 半透明底是 `docs/26 §F` 的第二类例外「仪表」**，论证与四条护栏写在 `ui/readout.css` 的文件头并登记进 §F：撤掉边框，这几行字就退回作品自己的排版，于是一件装置开口报数 —— 那比一条边违反得更彻底。底色是**字色兑水 12%**（`color-mix` + `--sb-on-stage`），所以跟着场景翻；算出来五套场景字压在面板上 **7.38:1（白展厅）~ 17.18:1（纸）**，面板对画面 1.24~1.36（`readout.test.ts` 从 CSS 里读那个百分比，抄一份的话调坏了也绿）。4Hz 刷新、定宽两栏（标签 `max-content` + 值 `6.5ch` 等宽 tabular），数字跳动不回流。`?readout=on|off`，网页版默认开、`?kiosk=1` 默认关、`?demo=1` 照常开（它不声称有摄像头，所以没有 `?preview=` 那条例外）。13 条测试。**实拍取证**（dev server，`?demo=1&theme=porcelain&clip=jumpingjacks`）：`scene=gallery` 白展厅下面板读作一块浅灰纱、标签与值都清楚、物种名牌「瓷 / Porcelain」在它**下面**不重叠（预留的 3.4rem 生效）；`scene=void` 深空下读作一块浅灰纱，实测读数 置信 0.95 / 关节 33/33 / 推理 31 Hz / 动能 1.955 / 舒展 0.39。**paper / tide / backlit 三套场景没有截图**，它们由上面那条算出来的对比度 + `stage-ink.test.ts` 的实测角上亮度覆盖 |
| **「它有没有看见我」左上角小屏幕 `src/ui/preview.{ts,css}` + 判据 `src/ui/preview-state.ts`** | `experimental` | 在这之前，观众站在摄像头前面**没有任何办法**知道自己有没有被看见：追踪一垮（逆光 / 离太近 / 半个人出画 / 摄像头对着墙），身体就只是站着不动，而"站着不动"和"作品坏了"在观众眼里是同一个画面。现在左上角有一块 168×126 的圆角小屏幕：底是**采集端自己那个 `<video>`**（不新开 `getUserMedia`、不新起 MediaPipe，那个元素本来就在解码，只是从没进过 DOM），上面叠帧循环**已经拿到手的**那副骨架（16 条边，脸上的点不画）。四种状态、只有三种说话：没摄像头 / 没有人 / 半个人出画或质量不够 / 好的（**好的时候一个字都没有**，docs/23 §S4）。质量判据**直接借 `core/refine.ts` 的 `qualityScale()`**，不另立门限 —— 两套置信度必然漂，漂了就会出现"小屏幕说正常、身体却在发木"（P21）。出画判据另算，因为半个人出画时 score 往往还很高。**圆角是 `docs/26 §F` 反面清单唯一一处被批准的例外**（它是画面里引用的一件硬件，不是 UI 面板），登记在 §F 与 `preview.ts` 文件头，只到 `.sb-see-screen` 为止。挂载点在 `main.ts` 的 4b 段 —— 观众**选完物种进到作品之后**，绝不提前（提前就等于把权限弹窗挪到第一眼，正是 `shell/entry.ts` 存在的理由）。`?demo=1` **永不挂**（没有摄像头却有别人的 landmark；挂着画骨架 = 观众一挥手骨架不跟 → 结论正好是"它没在看我"），`?kiosk=1` 默认不挂、`?preview=on` 可要回来。测试 `packages/app/test/preview-state.test.ts` 18 条，其中 4 条跑**真录制** `assets/demo/pose-walkturn.json`：那段录制里有 1.8 秒检出/丢失来回 **18 次**，憋话之后屏幕上只变 **1 次**（注释掉憋话 → 19 次，测试立刻红）。**出画那条判据在真录制上没有证据**（录制只存了 `world`，没有 `screen`），有一条测试专门钉住这个缺口。取证：`?debug=1&theme=char.line&preview=on`（浏览器面板禁 `getUserMedia`，所以验的是"没摄像头"那一态）—— 文案双语两行、`.sb-see-video` 不存在、骨架画布**逐像素全空**（没有幽灵骨架）、`border-radius: 10px`、`pointer-events: none`、rect `[24,24,168,187]`、HUD 已让到 `top:224` 不重叠。**真摄像头下的另外三态没验过**，见下面「明确还没验证的」 |
| **取景模式：上半身 ↔ 全身（`core/src/autoframe.ts` + `core/src/leghold.ts`，docs/49 §5）** | `experimental` | 同日第二轮按作品负责人的新方向落地。分类器 `full` / `upper` / `stepping-back`（漏桶式连续成立 + 1.2 秒冷却；头肩异常、刚进上半身后明确退后、退后完成三条纠错不等冷却），策略 `?framing=auto\|full\|upper` + 控件「取景」· C。只改输出侧：舞台中景（同机位只收窄 fov，画面高 = 身高 × 0.64，横 ±12cm / 竖 ±8cm 跟随，约 1 秒二阶临界阻尼，减少动态 0.15 秒）、腿 0.5 秒混成站姿并按脚落地、小屏 1.3× 裁切、引导与 WRN12 不为画面下边说话。景别进度保存速度，目标反向先刹再回；位置 / 速度限幅、视觉端点精确收口，`AUTOFRAME.maxStep` 同时守位置与速度变化，`/dev/framing.html` 显示速度曲线。**采集端一个像素没动**。2026-09-15 第三轮其余落地：降级 hold 只冻结跟随、小屏告警 0.2 秒限速退回；一个控制器（`stepFollow`：去抖 / 前馈 / 稳定延迟 / 死区 / 临界阻尼 / 限速）；身体的横向根偏移（躯干画面 x → 米，镜像，停在边上、跟丢 1 秒回中线、换人停住、多人让位）与侧边出画的话（`outLeft` / `outRight`，WRN12 同一把尺子）；`?camframing=auto\|on\|off`（只读 / 能请求时才请求 `faceFraming`，不进控件条）。2026-09-18 又补了画面空间纵向：可信 pelvis（近处裁胯时退到 chest）的 `screen.y` 与 human skeleton 的同名 world 关节同时立基线，screen 位移先扣掉 world 同步位移，避免把蹲起算两次；残差再经画面空间去抖 / 死区、按躯干尺度折米。已有基线时坏光冻结，短丢失冻结 / 长丢失归中；基线过期后，现代 screen 链的明确丢失或坏光继续归中，不再复活 world fallback。身份门或换人式大跳重立基线；全景 / 多人 / 减少动态不跟，只有没有 screen 字段的旧回放退回 world 信号。工作台同时显示双轴移轴，并与生产链同吃画幅、系统取景、上半身横移快档和同名 world 关节；HUD / trace 使用真实 reading / decision。测试覆盖同 world 只改 screen.y、screen+world 同步抵消、1.5% 画面高小位移、坏光 / 丢失 / 过期后坏光重现 / 身份跳变、反向、随机连续性和 15/30/60/120Hz 对照。**仍没接过真人摄像头；近距离 presence 已拆成整身平均或可靠躯干对，合成的 0.435 裁腿用例已通过，下一条门是用真机验证近 / 中 / 远距离、逆光和多人误检** |
| **自动取景（auto framing / Center Stage 那一类）** | `spec-only` | 2026-09-14 研究线：`docs/49-AUTO-FRAMING.md` §0–§4。其中"舞台相机不跟随、小屏以后再说"已被同日 §5 按新方向改写（见上一行）；"不裁 MediaPipe 的输入"没有变。**现在就该做的是现场检查项**：macOS Center Stage / Windows Studio Effects Automatic Framing / NVIDIA Broadcast Auto Frame / 云台人脸跟踪都对准脸、系统级对所有 app 生效，会在上游把腿裁掉；网页侧 `faceFraming` 约束仍在 Chromium prototyping（chromestatus 5129939115835392），今天检测不到。PTZ 约束 Chrome 87+、要求 UVC 绝对 PanTilt |
| **启动失败屏（docs/23 §S0）** | `stable` | 原来贴红色 `<pre>` + `err.message`。这是观众唯一会撞上的错误界面 —— 现在是满屏底色 + 居中并置的「稍等一下 / One moment」，详情全部进控制台。样式内联、不用 `innerHTML`：走到这里说明启动链断了，样式表本身可能就是断掉的那一环 |
| **`/about` 作品陈述页** | `stable` | 一屏之内答"它是什么"（五格的观众 90 秒带），再答"和 2019 年那件的区别在哪"（快回路 16ms / 慢回路 30–90s 并排）。慢回路一栏现在是**第三种状态「现场限定 ON-SITE ONLY」**：既不是"只写了设计"（两端都接上了、有测试有取证），也不是"已实现"（真实生成调用一次没打过，且线上版里是 404）。用同一个标记盖这两种情况，盖哪边都是撒谎 —— 所以加了一个。独立 chunk（6 KB JS + 4 KB CSS），不拖 1.5 MB 的 main。截图 `scratch/evidence/about-main.png`（1280×3900） |
| **品牌字体规范 `docs/27-BRAND.md` + 规范页 `/poster/brand.html`** | `stable` | 规范页由它所描述的那套系统**自己渲染**：每一条字号/行高/字距/颜色都从 `type.css` 的 computed style 现读。§6 把卡片、图标、进度条、toast 画出来再划掉。§1 当场量字体回退：字号/行高/字距/行框完全一致，**字形实宽 293.56 vs 311.03px 不一致** —— 值得守的主张从来不是"字形宽度一样"。截图 `assets/brand/brand-spec-page.png` |
| **叙事中文的楷书 `assets/fonts/LXGWWenKai/` + 覆盖面脚本 `scripts/font-coverage.mjs`** | `stable` | 两件事，一根因。**一：字表是算出来的，不是手抄的。** `NOTICE.md` 原来写「子集里 1,321 个汉字」，实测 **958** —— 子集化只做过一次，文案一直在长，叙事中文于是半楷半黑。现在脚本扫 `packages/app` 下的 CSS 找出所有用 `var(--sb-kai)` 的选择器（六条），按登记表取对应文案的汉字，与子集 cmap 对账；多一条没登记的选择器就报警。**二：字体本身换了。** 原来的芫荽 Iansui 是**繁体字身**（改自日文 Klee One），站里的文案是简体，「关 观 对 实 验 选 过 们 …」这 81 个字**上游 v1.020 的 cmap 里根本没有** —— 不是子集化漏掉的，重跑一百遍也救不回来。2026-09-13 换成**霞鹜文楷 LXGW WenKai Lite v1.522**（同为 SIL OFL 1.1，同样改自 Klee One，但补的是 GB 简体）：同一把尺子量出来**严格层 354/354、余量层 970/970，一个不缺**；子集 **232,124 字节 / 970 个码位 / 971 个字形**（比芫荽的 211,148 大 21 KB，换来的是那 81 个字），字表带整本 `i18n.ts` 的余量。**换字前必跑** `node scripts/font-coverage.mjs --upstream=<候选 TTF>`：「上游没有的·严格层」不是 0 就不要换进来。取证是**真浏览器逐字比像素**（Chrome 152，自开 `vite --port 5219` —— 机器上常年挂着的 dev server 可能是另一个 checkout，先 `curl -sI` 确认服务端给的是 232,124 字节；`document.fonts` 里 `LXGW WenKai` = `loaded`；同一个字用 `64px "LXGW WenKai", <grotesk>` 与 `64px <grotesk>` 画进 canvas，位图相同 = 没走楷书 —— 汉字两边都是 1em，比字宽量不出来；无头 Chrome 根本不加载这个 webfont，截图会骗人）：`/` 首屏立意句 **15/15**，`/about` **78/78**（换字前 61/78）。负对照「龘 鼯 麤」如期报落回，说明这把尺子会不及格 |
| **四张海报 `packages/app/poster/`** | `experimental` | A1 说明（把逆向结论掉头对准自己：印 1,642,496，再说它是原作 47,000 的 35 倍、而这正是它不值一提的原因，然后印分母 —— 10 件被人删掉的部件和逐条手写理由，全页唯一的颜色就在那 10 个 id 上）/ A1 阵列（21 张 anchor 其实全是同一个部位，海报就认这件事，并引 `docs/18` 的"辨识度住在整体剪影里，不在零件里"把局限变成来现场的理由；两个空格画对角线 —— 那两个条目的内容恰好就是"没有"）/ A1 立场（自评 1/6，收尾"这张海报会过期。那一天我们就换一张"）/ A4 票根（seed 栏空着，因为 `genome.ts` 保证同 seed 长回同一具身体 —— 它不是截图，是配方）。全部 PNG 在 `assets/brand/`，长边 ≥2000px。**`@page` 尺寸按规范写，但没有人真的导出过一张 PDF、也没有人真的印过** |
| **海报数字不许手打（`poster/build-data.mjs`）** | `stable` | 数字只有一个来源且是跑出来的。槽位表 `import` 自 `core/src/slots.ts` 正本，不留抄本 —— 去掉抄本时输出一字未变，这既说明它此刻是对的，也说明这类错**不会在出现的时候被发现**。脚本自己冒出来两条没人要求的结论并留在了海报上：`digitigrade`（自有 6 件）和 `wheelleg`（自有 1 件）的自有部件**全部被人工剔除**，现在整具身体借着 `porcelain` |

## 明确还没接上的

- **慢回路从没打过一次真的 Rodin。** 整条链在 `SLOW_FAKE=1` 下端到端跑通，
  但 image-to-3D 那一段（剪影当参考图、`creative` 模式、真实耗时）是 `Not run`：
  这个 worktree 里没有 `.env`，一次真调用要 0.5 credits。`docs/09` U10 因此还空着。

- **`mass` 的三个条目（coral / char.dumpling / char.ghost）不用它们自己的刚体件。**
  B 档换的是渲染器，一个槽位件都不实例化 —— 那 32 件花过 credits 的资产在画面上看不见。
  这是 mass 落地那一轮的取舍，不是新账。A 档的新拓扑没有这个代价（docs/18 §6）。
- **`radial` / `column` 没接过真人骨架。** 合成 A-pose 与三副合成姿态下因果成立，
  但真人追踪的抖动会怎么进到"弧半径"和"折叠角"里，没见过。radial 因承重链断开
  已只留显式开发预览；这是隔离风险，不是真人验收的替代。
- `mass` 里的旋钮（`res` 默认值、球间距、半径系数、`isolation`/`subtract`）按 `tuning.ts`
  的规矩本该住在 `tuning.ts`，同样因为冻结契约暂时留在模块里。

- **docs/23 里三处"规格写了但没做到"**（2026-09-13 全流程走查找出来的，本轮只修了前两条的邻居，
  这三条**没修**，因为它们各自要动别的线正在改的文件）：
  - **§S4「往后一点」**：`COPY.live.stepBack` 写好了，一个调用点都没有。判定"全身不入镜"
    要读 `stage/framing.ts` 的取景结果 —— 那条线这轮不归 UI 动。
  - **§S7 留念（6 秒 · `物种名 · seed 码` · 二维码 / 网页版分享卡片）**：
    `COPY.leave.keepsake` / `COPY.leave.seed` 写好了，没有调用点。溶解那一半是有的
    （`presence` 的 LEAVING），留念那一半整个不存在。这是一个功能，不是一处 UI 缺口。
  - **§S1 网页版「站到画面里」**：`COPY.attract.invite` / `inviteWeb` 没有调用点。
    目前网页版的对应物是入口展签的「开始」和运行中的「用我的摄像头」，
    覆盖了这条的大部分意图，但空场里没有那一行邀请。
  - 另外 **§S0「冷启动 > 8 秒 → 粒子团聚拢成人形轮廓」**：慢网上现在是加载态那三行，
    不是粒子。两者不冲突，但粒子那一版没做。

### 2026-09-18 · presence 与整身质量拆开

`packages/core/src/pose-signal.ts` 现在是“这一帧有没有人”的唯一判据：main 的相遇状态、webcam/replay 的无人降帧、
小屏、读数、自动取景、人数观测与选择页挥手都共用它。原 `RawPose.score > 0.5` 路径保留；整身平均不过时，只有成对肩 / 胯达到
`REFINE.qualityStart` 才是例外。空 landmark、全非有限坐标、统一低质量仍不通过。`13×0.95 + 20×0.1 = 0.435` 的近距离用例已证明：会进上半身、两轴跟随和人数观测；读数仍显示 `0.43`、整身质量告警仍在。

因此这条只修了“近距离人被画外腿平均成无人”，**没有验证真摄像头下的近 / 中 / 远距离、逆光与多人误检**；这仍是自动取景从 experimental 往 stable 走的下一道门。

## 明确还没验证的（见 `docs/09`）

U1 MediaPipe 轴向 · U2 单目深度可用性 · U3 现场灯光 · U4 material=None 的影响 · U6 主题内一致性（anchor 后）· U7 API 并发上限 · U9 目标机帧率 · U10 慢回路端到端耗时

已解决并回填：U5 bbox 约束 · U8 减面 · U11 百万面兜底 · U12 preview_render · U13 数组字段编码

**左上角小屏幕的三态（2026-09-13）**：真摄像头下的「没有人」「半个人出画」「好的」三种表现
**一次都没在真人面前跑过**。原因是环境而不是懒：能自动驱动的浏览器面板**整体禁掉了
`getUserMedia`**，另起一个带合成摄像头的 Chrome 跑出来是 12–15fps / cpu 41ms（软件渲染
的后台窗口），在那种噪声下量什么都没有意义（P21：问一句"东西坏了的话这个仪表会显示什么"，
答案是"和现在一样"）。判据本身有 18 条单测、其中 4 条跑真录制；**缺的是"摄像头 → 屏幕"
那一段的眼睛**。要验的三件，一个真人对着 `http://localhost:4173/` 三十秒就够：
①整个人在画面里 → **一个字都没有**，骨架贴在自己身上、镜像方向对（抬左手，小屏幕里也抬左手）；
②往前走到半个人出画 → 出现「往后退一点，整个人进画面」，退回去后字消失；
③走出画面 → 「站到画面里」，且**骨架必须消失**（不许留一副幽灵骨架）。
